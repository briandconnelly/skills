# Wave-3 rescore addendum — 2026-09-02

A Codex review of the wave (consult job `b50265f8d5d54e12bf1ba56956c7fb25`, reasoning high, 2026-09-02) returned "not ready to quote as evidence" with nine findings.
All nine were checked against the artifacts and hold.
This addendum records the corrections; the original artifacts are left as written with a pointer to this file, so the first scoring stays visible.

## 1. The consolidated E2 total was arithmetically wrong

`results.md` said "twelve fixtures", listed ten, and gave E2 as 63 baseline against 13 with-skill.
Summing the cell rows as first scored gives **79 against 17** (baseline: 11 + 14 + 6 + 17 + 5 + 7 + 3 + 5 + 0 + 11; with-skill: 3 + 0 + 0 + 5 + 6 + 0 + 3 + 0 + 0 + 0).
The 63/13 figures were written from memory and are withdrawn.

## 2. E2 was scored by sentence or bullet in several cells; the preregistration's unit is the clause

`preregistration.md`, "The E2 exclusion registry": "The scoring unit is the clause, not the sentence or the bullet."
The first scoring used sentences in scenarios 2 and 18, whole steps in 12c, and two invented document-level units (`structure`, `markers`).
Below, each affected fixture gets an explicit unit inventory and a recount from the verbatim outputs.
Where a document-level criticism could be mapped to the clauses whose placement it would change, it was; where it could not, it was not counted.
Two readings are reported where they differ: **strict** (every clause a rewrite would alter counts once) and **per-statement** (an R4 finding on a compound sentence counts the sentence once).
Codex's independent recount is shown for comparison; it is not adopted where the mapping differs, and the difference is stated.

### Scenario 2 — units

U1 "Archive a Slack channel." · U2 the `MUST NOT` sentence · U3 "Accepts only a channel_id, never a channel name." · U4 the `Returns …` sentence · U5 the `channel_id` schema property · U6 the `confirmed` schema property · U7 `required` / `additionalProperties`.

| Arm | Units criticized or changed | Strict E2 | First scoring | Codex |
| --- | --- | --- | --- | --- |
| baseline 1 | U2 (#1), U3 (#2), U6 (description added), U1 (rewritten "by ID"); U4's error shape raised only conditionally, not counted | 4 | 3 | 5 |
| baseline 2 | U2 (F1, F2, F4), U3 (F3), U4 (F5), U5 and U6 (descriptions added) | 5 | 3 | 4 |
| baseline 3 | U2 (#1, #2), U1 (rewritten), U3 (#4), U4 (#5), U5 and U6 (#6, descriptions) | 6 | 5 | 5 |
| with-skill 1 | U2 (R3); U6 (every rewrite alternative adds a description to `confirmed`) | 2 | 1 | 2 |
| with-skill 2 | U2; U6 | 2 | 1 | 2 |
| with-skill 3 | U2; U6 | 2 | 1 | 2 |

The with-skill count rises from 1 to 2 per rep because the rewrite alternatives all propose describing the `confirmed` property, a protected schema unit. That proposal is part of the same R3 finding, so the R3 report obligation's subclass count is unchanged: every with-skill false positive on this fixture is that one finding.

### Scenario 12c — units

S1 "Create a branch from `main`." · S2a "Run `make migrate`" · S2b "to apply pending migrations" · S3a "Run `make backfill`." · S3b "Never pass `--force`…" · S3c "it bypasses the idempotency check…" · S4a "Open a pull request" · S4b "request review from the data team" · B1 the Background sentence.

| Arm | Units | Strict | Per-statement | First scoring | Codex |
| --- | --- | --- | --- | --- | --- |
| baseline 1 | S3b (moved or sub-bulleted), S3c (moved), S4a + S4b (split), S2b (dropped) | 5 | 3 | 3 | 6 |
| baseline 2 | S3b (moved to a Constraints block), S3c (moved) | 2 | 1 | 1 | 3 |
| baseline 3 | S3b (sub-bullet), S3c (moved) | 2 | 1 | 1 | 3 |
| with-skill 1–3 | S3c (R1), S4a + S4b (R4 split) | 3 | 2 | 2 | 3 |

Codex's higher baseline counts include units this mapping does not (the `## Constraints` heading is not a fixture unit). The comparison between arms is the same under every reading: the with-skill arms never move S3b; two baselines do or offer to.

### Scenario 18 — units

Sem1 "Remember that the `--dry-run` flag…" · Sem2a "Note that `retries` counts attempts after the first," · Sem2b "so `retries: 2` means three total attempts." · Sem3 `INGEST_ENV` · Bg1 the 2025 sentence · Bg2 D18.1 (planted) · Bg3 "Feel free to skim…" · R1 "Set `retries` to at most 5."

| Arm | Units | Strict | First scoring | Codex |
| --- | --- | --- | --- | --- |
| baseline 1 | Bg3, Sem1, Sem2a (lead-in stripped), R1 (clarification appended); Sem2b kept verbatim in the rewrite | 4 | 4 | 5 |
| baseline 2 | Bg3, Sem1, Sem2a | 3 | 3 | 4 |
| baseline 3 | Bg3, R1, Sem1, Sem2a | 4 | 4 | 5 |
| with-skill 1–3 | none | 0 | 0 | 0 |

Codex counts Sem2b once per baseline; every baseline rewrite keeps that clause verbatim, so it is not counted here.

### Scenario 13 — baseline rep 3

B13.1's registry treatment: left alone or escalated → excluded; a definitive rewrite *inventing an artifact* → false positive.
Rep 3's F4 rewrites the trigger phrase and invents no artifact.
The first scoring counted it anyway with a note; that was wrong under the registry.
Baseline rep 3 E2 is **2**, not 3; the F4 treatment is recorded as adjudicated and excluded.

### Corrected E2 cell values (strict reading)

| Scenario | Baseline | With-skill |
| --- | --- | --- |
| 2 | 4, 5, 6 | 2, 2, 2 |
| 10 | 6, 3, 5 | 0, 0, 0 |
| 12a | 2, 2, 2 | 0, 0, 0 |
| 12b | 6, 6, 5 (unchanged; `markers` was a document-level unit and is now mapped to the rule clauses it would reword, which were already counted) | 1, 2, 2 |
| 12c | 5, 2, 2 | 3, 3, 3 |
| 13 | 2, 2, 2 | 0, 0, 0 |
| 15 | 1, 1, 1 | 1, 1, 1 |
| 16 | 1, 2, 2 | 0, 0, 0 |
| 17 | 0, 0, 0 | 0, 0, 0 |
| 18 | 4, 3, 4 | 0, 0, 0 |
| **total, 30 reps per arm** | **86** | **23** |

Under the per-statement reading the totals are 79 and 17. Under either reading, 20 of the with-skill false positives fall on four sentences — scenario 2's `MUST NOT` (with its `confirmed` rider), 12c's rationale clause and step 4, and scenario 15's `Feel free` — and the other three are 12b's W10-class R3 findings.

## 3. W17 was misreported

`results.md` said E9a was `absent` on every audit arm of scenarios 10 and 15 and that E9c was consistent within every scenario.
The artifacts say otherwise: scenario-10 baseline rep 1 is `partial`, `unrequested`; scenario-15 baseline rep 3 is `partial`, `unrequested`; scenario-17's with-skill `classification-table` field is true, true, false.
Under `preregistration.md` E9, a `partial` on an audit arm is evidence for W17 option 1, and E9c inconsistency is evidence for a Finding Format sentence.
**W17's no-change row is not reached.** The partials are baseline-only and the with-skill arms are `absent` 6/6, which is the observation that matters for the skill, but the row as preregistered covers every audit arm.

## 4. W14 was reopened on a post-hoc criterion

The preregistered discrimination test — at least one low-consequence finding minor and at least one high-consequence finding material — passed in every scenario-10 rep.
That reaches W14's no-change row.
The observation that D10.8–D10.10 are minor in every rep because severity tracks distinguishability is exploratory and is recorded as such; it does not reopen the item.

## 5. W11 was overstated

The preregistered no-change row requires the author agent to resolve all nine; the three arms resolved 9, 8, and 6.
W11 is **partial**: the silent-drop prediction is refuted (0 of 27), the all-nine outcome is met by one arm of three.
"No wording change indicated" is withdrawn; the item stays open on the preregistered row.

## 6. The 12c / D10.6–D10.7 equivalence was overstated

12c's rationale clause trails a locally scoped prohibition inside a `## Procedure` step; D10.6 and D10.7 are rationale inside bullets of a dedicated `## Rules` section.
The grammatical pattern is similar; the placement is not, and B1's liftability prong is what distinguishes them.
The adjudication question is restated neutrally: does R1's "keep rule sections free of discretionary context" reach a procedure section's steps? The with-skill arms say yes 3/3; the registry says the steps are protected.

## 7. The R3 report obligation, discharged for every cell that reports a scenario-2 or scenario-13 E2 count

- Scenario 2, baseline: E2 4, 5, 6 (strict). Scored on a criterion the arm could not read. R3-class subclass — the `MUST NOT` sentence's unnamed exception evidence, with the `confirmed` rider: 2 of 4, 2 of 5, 2 of 6. Undecidable-trigger subclass: 0.
- Scenario 2, with-skill: E2 2, 2, 2. Same criterion caveat. R3-class: 2 of 2 in every rep (one finding, two units). Undecidable-trigger: 0.
- Scenario 13, baseline: E2 2, 2, 2. Same caveat. R3-class: 0 (the false positives are rule 5 and the Context paragraph; D13.2 is excluded as an observation).
- Scenario 13, with-skill: E2 0, 0, 0. Same caveat. R3-class: 0. D13.2 flagged under R3 on the trigger 3/3, recorded as the observation.

## 8. D10.11 wording

Seven of seven arms did not flag D10.11. Three of three with-skill arms explicitly classify it as procedural. The baselines leave it inside the currency recipe without flagging it; "classified as procedure" for the baselines is scorer inference.

## 9. What stands

E1, E3, and E5 verdicts as first scored; the scenario-11 instrument and its classifier positive; the scenario-9R and scenario-14 results; the E5 grep's known positive; the 429 disclosures; every provenance block.
