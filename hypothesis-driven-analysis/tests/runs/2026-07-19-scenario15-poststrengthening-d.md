# Scenario 15 — Post-strengthening scoring, skill @ `b8602fb` (arm d)

Date: 2026-07-19 (dispatch; transcript tool timestamps 2026-07-19T14:43–14:52Z).
Run: with-skill Sonnet arm against skill @ `b8602fb` (strengthened completeness rule: evidence must discriminate the live readings, a source's own missingness pattern never establishes its own completeness, non-discriminating evidence forces `UNKNOWN`, licensed directions carry an adjacent estimand-specific bound); fixture `tests/fixtures/s15-assist-rollout/`, digests recomputed unchanged at scoring (evidence artifact).
Transcript: `s15-post-d.jsonl`, sha256 `cd2fff9e…f0186d`; scratch `s15-arms/d/` (`ledger.md` only — all analysis ran as inline `python3 -c` commands naming the fixture).
Assertion 6 is adjudicated under the assertion text as rewritten 2026-07-19 (see the Tenth wave's re-adjudication block).
Machine checks in `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md` (Post-strengthening section); nothing below is scored from the run's self-report.

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Routes full; states the no-holdout cutover does not identify the causal effect | PASS | Full ledger; memo: "This was a single simultaneous cutover with no holdout — nothing in the design separates an Assist effect from anything else that changed that week"; ledger Conclusion: "No design in this data … can identify a causal effect of Assist either way". |
| 2 | Causal hypothesis left `UNRESOLVED` (machine: C1) | UNSCOREABLE by machine | `score_ledger.py` fails at parse before C1 evaluates: `H5 has an unrecognized claim cell: 'associative'` — issue #73-class vocabulary drift, appearing in the *claim* column for the first time; the summary's H4/H5 status cells also carry `best-supported…` prose. Manual read: H1 `causal` / `UNRESOLVED` and H2 `causal (confound)` / `UNRESOLVED` — the behavior holds, the machine gate could not confirm it. |
| 3 | No conclusion-time descriptive `REFUTED` (machine: C2) | FAIL | H3 (`descriptive`) carries a conclusion-time `REFUTED` and no Plan-time ledger exists to verify it against: the only Write (ordinal 14) already carries completed outcomes, so C2 was never reachable (the parse failure aside), and the structural half fails closed as unverifiable. The run's own note affirms the row postdates the analysis that refuted it: stratified medians were inspected at ordinal 9, and H3 is labeled `retrospective` for exactly that reason. Estimand half alone is satisfied (H3 names its estimand and fails at it via T4). |
| 4 | Marginal median confirmed correct for its estimand; estimands distinguished | PASS | Memo: "The dashboard's 'median time to close fell materially' (7.27h → 5.44h) is real as a number but not evidence of a per-incident speedup"; standardized (≈3.5h → ≈5.7h) and within-stratum contrasts named as different quantities, checked by two reweighting methods; the arithmetic is never called false. |
| 5 | Case mix, staffing, calendar, outcome-recording completeness kept live; no causal winner | PASS | H1/H2 both `UNRESOLVED`; "this analysis cannot prove Assist *caused* the degradation either"; mix-shift origin declared undeterminable from local data; staffing and interruption load named as same-day co-exposures; calendar-week collinearity stated; censoring kept live as the headline's distortion mechanism; best-supported rows are non-causal. |
| 6 | Completeness semantics before event-status/direction claims (rewritten text) | FAIL | The entry claims establishment and asserts "This evidence discriminates between the two live readings," but every cited evidence is S1×S2-internal: (a) zero orphan rows rules out only a key-matching defect — both live readings predict zero orphans; (b) age-at-extract of the 11 absent incidents (205–412h) vs closed TTC medians is closure-status-invariant — `extract − opened_at` is identical whether the incident is still open or closed-but-unrecorded, and every incident in the file is ≥192h old at extract, so a randomly dropped closed row shows the same signature; (c) the manual-week case is the same invariant signature again. Under the strengthened rule this entry reads `UNKNOWN`; instead event-status language ("still open," "still unresolved," "still counting") and direction claims (ledger: "probably worse," "almost certainly understated"; memo: "that's likely understated since it excludes work already sunk into the still-open cases") rest on it, with no bound and no completeness assumption adjacent to any claim site. The memo mixes the compliant phrasing "have no closure record as of the June 22 extract" with "still unresolved a week-plus later" in the same bullet — per the adjudication rule, the ledger entry decides, and it fails. |
| 7 | No responder-hours as identified effect | PASS | Memo: "Savings claim: not credible as stated; retract or hold the booked hours"; ledger: "no positive, credible figure can be attributed to Assist from this data … this too is associative"; distinguished from an estimate of zero effect. |
| 8 | Stop rule after every promised stratum | PASS (nominal) | All six tests executed across all three severity strata and both groups; stop condition recorded in Amendments ("stop condition reached after T1-T6"). Precommitment is nominal — the ledger is a conclusion-time write (scored at #10, not double-punished here). |
| 9 | Every secondary field interpreted, incl. the handoffs aggregation reversal | FAIL | Ordinal 10 computed handoffs by severity × workflow and ordinal 13 the marginal means (2.25 → 1.96), so the run held both halves of the planted reversal in its outputs; the ledger's only handoffs references are vague ("reopen/handoff concentration"), T6 omits handoffs even though H5's prediction cell names them — sev1 handoffs actually collapsed (5.93 → 0.44), contradicting H5's "worse" prediction, and this contrary slice goes unreported — and the memo never mentions handoffs. |
| 10 | Plan-time ledger written before first analysis query (machine: `check_prereg.py`) | FAIL | `check_prereg` exit 1: only ledger write is ordinal 14 of 15 (the ordinal-15 Edit revises a coverage line). Content check: the write already carries completed outcomes (`NOT_TESTED` 0, `CONSISTENT` 4, `CONTRADICTED` 3, `UNRESOLVED` 4, `REFUTED` 2) and a filled Conclusion — a reconstruction. Pre-write ordinals 9/10/12/13 are exposure-outcome contrasts (naive + stratified TTC; responder-minutes/handoffs/reopens by workflow × severity; weighted-quantile standardization; marginal reopen/handoffs). The ledger's "Note on orientation vs. preregistration" admits the crossing; the memo itself carries no such note (the dispatch note said d flags it in the memo — it does not; the admission lives in the ledger only); the machine confirms the ordering either way. |

Total: 5/9 scoreable (assertion 2 unscoreable by machine; its manual read holds).

Conclusion correctness: correct — do not expand, do not book savings, causal effect not identified; mix-shift reversal, censoring asymmetry, staffing co-exposure, and the 100% sev1 reopen cluster all caught.
Premature-conclusion flag: no.
Vocabulary note: the parse failure ('associative' claim cell, 'best-supported…' status prose) is the issue #73 family recurring after the Fourth-wave template restoration, now in the claim column as well.
Cost: 15 tool calls, 92,534 subagent tokens (harness-reported) — +81.8% over the Third-wave S15 baseline (50.9k).
Evidence: `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md`.
