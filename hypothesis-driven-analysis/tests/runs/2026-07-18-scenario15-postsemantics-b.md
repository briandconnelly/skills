# Scenario 15 — Completeness semantics, post-`c7a3179` scoring (arm b)

Date: 2026-07-18 (dispatch; transcript tool timestamps 2026-07-19T03:46–03:54Z).
Run: with-skill Sonnet arm against skill @ `c7a3179`; fixture `tests/fixtures/s15-assist-rollout/`, digests verified unchanged by the controller at dispatch and recomputed at scoring (evidence artifact).
Transcript: `s15-post-b.jsonl`, sha256 `3227e51e…c6f3d`; scratch `s15-arms/b/` (`ledger.md` only — all analysis ran as inline Bash heredocs).
Machine checks in `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md`; nothing below is scored from the run's self-report.

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Routes full; states the no-holdout cutover does not identify the causal effect | PASS | Full ledger; memo: "this data cannot establish that Assist itself caused the within-severity slowdown … not fully identified from a single unreplicated pre/post cutover with a concurrent staffing change". |
| 2 | Causal hypothesis left `UNRESOLVED` (machine: C1) | UNSCOREABLE by machine | `score_ledger.py` fails at parse before C1 evaluates: three status cells read `best supported` (H2/H3/H5) — outside the template's restored `REFUTED`/`UNRESOLVED` vocabulary; issue #73-class recurrence, the Third wave's exact defect back after the Fourth-wave fix. Manual read: H1 `causal` / `UNRESOLVED`, "cannot be marked REFUTED by the contrast alone" — the behavior holds, the machine gate could not confirm it. |
| 3 | No conclusion-time descriptive `REFUTED` (machine: C2) | PASS (vacuous) | Zero `REFUTED` rows anywhere in the summary (manual read; the machine never reached C2 for the same parse failure); no Plan-time ledger existed to check against regardless (see #10). |
| 4 | Marginal median confirmed correct for its estimand; estimands distinguished | PASS | Marginal medians computed and verified at ordinal 8 (436.0 / 326.5 min); memo treats the headline as arithmetic fact and reclassifies it ("an artifact of case-mix and censoring, not a treatment effect"); never calls the dashboard number false. |
| 5 | Case mix, staffing, calendar, outcome-recording completeness kept live; no causal winner | PASS | Staffing co-intervention (H4 `UNRESOLVED`), mix shift, censoring all live; "cannot cleanly separate 'caused by Assist' from 'caused by more staff' or 'caused by whatever changed what kind of incidents arrived that week'"; reopen pattern raised as closure-validity signal. |
| 6 | Completeness semantics before event-status/direction claims | FAIL | The field is present but its cited evidence cannot establish the reading: "established by the closed-incident age benchmark above and by the fact that `incidents.csv` (S1) already lists these 11 incidents by ID". Age-at-extract is `extract − opened_at` for an absent-row incident under *either* reading (still open vs closed-but-unrecorded), so the benchmark is closure-status-invariant; S1 listing an incident says nothing about whether its closure was recorded in S2. The entry then over-claims — "licenses treating them as right-censored survivors, not as an unknown-direction missingness problem" — and the run's strongest direction language rests on it (Data Validity: the closed-only comparison "has the wrong sign built in by construction"; memo: the open incidents "can only push Assist's true sev1 performance further behind"). This is the Third-wave failure (elapsed-age argument assuming absent = still running) wearing the new field as a label. |
| 7 | No responder-hours as identified effect | PASS | "The honest range for 'hours credibly attributable to Assist' spans from 'no measurable saving' to 'a measurable increase in burden on hard incidents'" — a range under non-identification, distinguished from a zero estimate; booking refused. |
| 8 | Stop rule after every promised stratum | PASS (nominal) | All five planned tests plus both promised held-out slices executed; no stratum skipped. Precommitment is nominal — the ledger is a conclusion-time write (scored at #10). |
| 9 | Every secondary field interpreted, incl. the handoffs aggregation reversal | FAIL | Ordinals 9/11 computed marginal handoffs (2.25→1.96) and per-stratum handoffs (sev2 3.01→3.95, sev3 0.54→1.52 worsening), so the run held the full reversal in its outputs; the ledger reports only the sev1 slice (5.93→0.44, flagged as possible premature closure) and the memo omits handoffs entirely — the aggregation reversal is never surfaced. |
| 10 | Plan-time ledger written before first analysis query (machine: `check_prereg.py`) | FAIL | `check_prereg` exit 1: only ledger write is ordinal 14 of 14; all 11 listed pre-write touches include full outcome analysis (closed-only medians by workflow at ordinal 8, stratified medians at 9, standardization at 10, bootstrap CI at 13). Content check: the write carries completed outcomes (0 `NOT_TESTED`) — a reconstruction. The run's own note ("orientation … crossed from 'orienting' into 'inspecting the cause-outcome relationship'") is confirmed by the machine evidence. |

Total: 6/9 scoreable (assertion 2 unscoreable by machine; its manual read holds).

Conclusion correctness: correct — do not expand, do not book savings, causal effect not identified; ground-truth features caught, including the held-out day-by-day step-function and intake-channel corroboration no other arm ran.
Premature-conclusion flag: no.
Cost: 14 tool calls, 94,222 subagent tokens (harness-reported) — +85.1% over the Third-wave S15 baseline (50.9k).
Evidence: `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md`.
