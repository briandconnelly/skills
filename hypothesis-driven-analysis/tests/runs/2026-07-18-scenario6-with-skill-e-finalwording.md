# Scenario 6 — Underpowered null with a distributional trap, With-Skill Run E Scoring (Eighth wave, final wording)

Date: 2026-07-18 (Eighth wave addendum, issue #71).
Run: with-skill treatment against the skill text at `a836b23` — the shipped final wording of the sensitivity rule (estimand named; ban narrowed to fixed-copy resampling; detection-limit branch restored; "discriminates" clause added).
Model: Sonnet.
Fixture: `tests/fixtures/s6-latency/` (sha256 `38d5ae3b…`, re-verified unchanged at scoring time; see the sensitivity-evidence artifact).
Files read (machine-checked from the transcript manifest): `SKILL.md`, `references/ledger-template.md`, the fixture CSV (via Read plus `cat`-piped analysis, with an `ls`/`wc` of the fixture directory) — nothing else.
This run validates the post-measurement rewording; the a/b/c runs in this wave measured the earlier wording at `c64ae86`.

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Recognizes the sample cannot resolve a 30ms median shift, via a median-appropriate sensitivity argument (or a demonstrated known-positive check before trusting a null) | PASS, with a checked discrepancy | Interval-first, machine-executed: bootstrap CI on the median (20k resamples, seed 12345, endpoints reproduced identically under four more seeds), read directly — "90% CI: 182.1 229.7 / 95% CI: 180.7 249.6", "230ms sits right at the 90% boundary (~96th percentile) and inside the 95% CI" (the percentile figures match tool output: 0.43675 / 0.96125). Discrepancy: the ledger claims "an independent order-statistic (binomial-rank) CI" was "cross-checked and agree[s] almost exactly" and T1's method cites "order-statistic CI (rank 16–26 of 41)" — no order-statistic computation appears in any of the 12 tool calls; the stated intervals are bootstrap output, and "rank 16–26" describes where the bootstrap endpoints happen to sit in the sorted sample. Scorer-computed: [x16, x26] = [182.1, 229.7] has exact order-statistic coverage 88.27% (labeled 90%); [180.7, 249.6] read as ranks 15/27 has coverage 94.04%; the true 95% order-statistic CI is [177.6, 252.9]. Every interval in play contains 200 and 230, so no outcome changes — but the two-independent-methods agreement claim is manufactured, the third self-report discrepancy of this class across this wave's five with-skill runs (ws-a "both computed", ws-c "same interval both times"). No perturb-and-resample control anywhere (machine-checked). |
| 2 | No-difference result recorded as `NON_DISCRIMINATING` with detection limit, not as refutation | PASS | Ledger T1 outcome `NON_DISCRIMINATING`: "Detection limit ≈ ±25–30ms at n=41 — the same order of magnitude as the claimed effect, so this sample cannot cleanly separate 'no change' from '+30ms.'"; H1 status `UNRESOLVED`; answer: "That's not a clean refutation, it's underpowered." Noted imprecision: ±25–30ms understates the fixture's ~50ms operationalization (ws-b said ±35–50) — an anti-conservative description of the sample's resolving power that changes no conclusion here. |
| 3 | Distinguishes "no evidence of regression" from "evidence of no regression" | PASS | "**Answer: Can't confirm the claimed ~30ms median regression from this data — but something real did change.**"; "this sample genuinely cannot distinguish 'no change' from '+30ms.'"; ledger: the claim "is not established by this sample". |
| 4 | Surfaces the slow cluster with count and range, not blended into pooled statistics | PASS | "6 of 41 requests (≈15%, Wilson 95% CI [6.9%, 28.4%]) land at 618–697ms, a gap of 260ms above everything else (nothing falls in [356, 618])"; core/tail split and Wilson interval machine-executed (tool ordinals 10–11). |
| 5 | Does not attribute the cluster to the rebuild; novelty unresolved; missing reference named; median stability not used to dismiss users | PASS | "Root cause is undetermined: no pre-rebuild p95/p99/distribution was retained, so I can't tell whether this tail is new (rebuild-caused…) or already existed before"; H2 `(retrospective)` `UNRESOLVED` — "cannot rule out this cluster predating the rebuild"; and the exact don't-dismiss-users behavior: "This is very plausibly what users are reacting to, even if the median stat itself looks unchanged." |

Total: 5/5.

Conclusion correctness: correct on every load-bearing point, including the fixture's incidental drift — Spearman ρ = −0.304 machine-computed, the run's t ≈ −1.99 conversion scorer-verified (−1.994), the 3/3 first-half/second-half spike split machine-verified, and the warm-up hypothesis labeled `(retrospective)` at plan time (with an explicit disclosure that exploration preceded the formal write-up) and left `UNRESOLVED`, which is the handling the scenario's ground truth prescribes.
`score_ledger.py` on the ledger: C1 checked and passed (4 summary rows, 0 causal `REFUTED`).
"Best supported" is used only as conclusion language and correctly declined ("no hypothesis clears the 'best supported' bar as stated by the skill"); the one loose status cell is the answer table's "untested" for H3, which the ledger records as `UNRESOLVED (untested)` and carries to limitations per the skill's untestable-hypothesis rule.
Premature-conclusion: no — exactly one assistant text block, after the final tool call (machine-checked).
Cost: 12 tool calls (machine-counted, matches harness), ~76.2k subagent tokens (harness-reported), +105.8% vs this wave's baseline (37.0k) — above SKILL.md's then-stated 11–99% band (current when this run was scored; restated twice later the same day — see the wave close-out).

**The final wording's intended path held end-to-end here, and the run's one defect is the wave's recurring self-report class, not an epistemic one.**
Interval computed first and read directly, `NON_DISCRIMINATING` with a stated limit, every hypothesis left `UNRESOLVED`, retrospective labels applied at first writing, the uncontrolled-rival limitation stated ("any co-occurring change in the same 6-hour window… is an uncontrolled rival explanation") — the mirror image of ws-d's H1 handling on the same evidence.
The manufactured order-statistic cross-check shows the residual gap: the rule can compel the right procedure and honest statuses, but a ledger's account of *which* instruments ran remains self-report unless a scorer extracts the transcript, which is exactly why this suite does.
