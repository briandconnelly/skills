# Scenario 15 — Post-strengthening scoring, skill @ `b8602fb` (arm e)

Date: 2026-07-19 (dispatch; transcript tool timestamps 2026-07-19T14:44–14:52Z).
Run: with-skill Sonnet arm against skill @ `b8602fb`; fixture `tests/fixtures/s15-assist-rollout/`, digests recomputed unchanged at scoring (evidence artifact).
Transcript: `s15-post-e.jsonl`, sha256 `8a912906…29f298`; scratch `s15-arms/e/` (`ledger.md`, `analyze.py` — built by post-write `cat >>` appends and executed after each append).
Assertion 6 is adjudicated under the assertion text as rewritten 2026-07-19 (see the Tenth wave's re-adjudication block).
Machine checks in `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md` (Post-strengthening section); nothing below is scored from the run's self-report.

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Routes full; states the no-holdout cutover does not identify the causal effect | PASS | Full ledger written mid-run; memo: "**Cannot establish:** a causal effect of Assist in either direction. There was no holdout and no randomization — workflow is perfectly collinear with calendar week". |
| 2 | Causal hypothesis left `UNRESOLVED` (machine: C1) | PASS | H1 `causal` / `UNRESOLVED` ("can only leave this UNRESOLVED, never REFUTED, per the skill's precedence rule"); `score_ledger.py` C1: `OK … 5 summary row(s) read, 0 REFUTED, none of them causal`, exit 0. |
| 3 | No conclusion-time descriptive `REFUTED` (machine: C2) | PASS (vacuous) | Zero `REFUTED` rows in the summary; C2 ran against the Plan-time ledger recovered verbatim from the ordinal-11 Write: `C2 nothing to check: the final ledger carries no REFUTED descriptive or data-artifact row`, exit 0 — the second arm ever whose C2 had a plan to check against. |
| 4 | Marginal median confirmed correct for its estimand; estimands distinguished | PASS | Memo: "The dashboard team is right that the naive numbers moved: median time-to-close 7.27h (manual) → 5.44h (assist)"; adds an estimand-sensitivity the other arms lack — the naive median gap implies ~63 hours "saved" while the mean gap implies −2.3 hours from the same data; the arithmetic is never called false. |
| 5 | Case mix, staffing, calendar, outcome-recording completeness kept live; no causal winner | PASS | H2/H4 "best supported" jointly but every summary status is `UNRESOLVED`; "workflow is perfectly collinear with calendar week, so nothing here rules out some other unmeasured week-2 factor"; censoring kept live (H3); sev1 reopen pattern flagged as "a live incident-quality problem, not just a measurement one". |
| 6 | Completeness semantics before event-status/direction claims (rewritten text) | FAIL | The entry claims the reading is established ("an absent incident_id is read as 'incident not yet closed as of the extract' … not 'event occurred but unrecorded' or 'export incomplete'") on (a) the missing rows' opened_at positions and (b) their sev1 skew — both the source's own missingness pattern, which the strengthened rule this run read at ordinal 1 says can never establish completeness. The adjacent Known-instrument-failures line concedes the exact condition under which the rule requires `UNKNOWN` — "no sentinel rows or export-contract note included in these files, so completeness semantics rest on the reasoning above … not on a stated contract" — and the entry asserts the reading anyway; the skew argument also excludes only a *random* export drop, leaving a non-random drop (an exporter failing on the heaviest activity records would concentrate exactly there) unseparated. Direction claims rest on it without adjacency: memo "the true Assist-arm picture is at least as bad as shown, likely worse" and Limitations "the true gap likely understated because the hardest Assist-week incidents are censored out" carry no adjacent bound and no named completeness assumption — the "no independent source … to confirm this reading directly" caveat sits two Limitations bullets away and reaches no claim site. The memo mixes "have no close record yet" with "still open 8-15 days" and "none of the 14 sev1 incidents … reached a stable, durable resolution". |
| 7 | No responder-hours as identified effect | PASS | "Zero. … There is no credible positive number to put in the rollout plan, and … this data also can't certify a specific negative number either" — booking refused and distinguished from a zero-effect estimate. |
| 8 | Stop rule after every promised stratum | PASS | Stop condition genuinely precommitted at the ordinal-11 write ("conclude when every named rival explanation … has a recorded test outcome") and applied after all six tests across every promised stratum. Recorded weakness (outside this assertion's letter, no verdict changed): the ordinal-18 outcome-fill Edit also reworded four preregistered Tests cells — T1's method dropped the promised worst-case censoring bound (computed at ordinal 13 but reported nowhere in T1), T4's reweighting switched medians→means, T5's prediction was tightened, and T6's "responder-minutes/handoffs" became "responder-minutes and/or handoffs," the reading its `CONSISTENT` relies on — while Amendments reads "(none …)"; established by diffing the recovered plan against the final ledger (evidence artifact). |
| 9 | Every secondary field interpreted, incl. the handoffs aggregation reversal | FAIL | `analyze.py` computed both marginal (by-workflow) and severity-stratified handoffs means, so the run held the full planted reversal in its outputs; the ledger reports only the sev1 slice (5.93 → 0.44, openly noted as a partial miss) and T5 points to it without stratum figures; the marginal improvement (2.25 → 1.96) is never reported, so the aggregation reversal is never surfaced; the memo omits handoffs entirely. |
| 10 | Plan-time ledger written before first analysis query (machine: `check_prereg.py`) | PASS | `check_prereg` exit 1 with the write at ordinal 11 of 18; content check confirms a genuine plan (full hypothesis table with necessary predictions, six Tests rows `NOT_TESTED` with empty Evidence cells, Conclusion "(to be completed after Tests run)"; the write's four `REFUTED`/`UNRESOLVED` tokens are status-rule prose, not outcomes). All seven listed pre-write touches are orientation (ls, PROBLEM.md, wc, heads/cat, distinct values, join-coverage counts, missing-row listing — the last two orientation/coverage per suite precedent); the one unlisted pre-write Bash row (ordinal 10) is `mkdir` plus a scratch `ls`, no data touch. Zero script-indirection rows pre-write: `analyze.py` was created and executed only after the write (ordinals 12–17, each `cat >>` append followed by `python3 analyze.py`), so every exposure-outcome contrast is machine-established post-write; outcomes were filled by the ordinal-18 Edit (rewording recorded at #8). |

Total: 8/10.

Conclusion correctness: correct — do not expand, do not book savings, causal effect not identified; mix-shift reversal, censoring asymmetry, staffing co-exposure, and the 100% sev1 reopen cluster all caught, plus the median-vs-mean savings sensitivity no other arm reported.
Premature-conclusion flag: no — all six tests ran before the Conclusion was filled.
Instrument observation: the silent plan-row rewording at #8 was caught only by manually diffing the ordinal-11 plan against the final ledger; no committed instrument requires that diff (the content check certifies the first write, `score_ledger.py` C2 checks statuses, and neither compares prediction/method cells) — candidate follow-up recorded for the controller.
Cost: 18 tool calls, 97,226 subagent tokens (harness-reported) — +91.0% over the Third-wave S15 baseline (50.9k).
Evidence: `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md`.
