# Transcript evidence — Tenth wave, S15 completeness semantics and the preregistration instrument's first live outing

Three with-skill Sonnet arms dispatched 2026-07-18 against skill @ `c7a3179` (fixture digests verified unchanged by the controller at dispatch), scored in `tests/runs/2026-07-18-scenario15-postsemantics-{a,b,c}.md`.
Transcripts were snapshotted to scorer scratch and processed with the committed instruments — `tests/extract_evidence.py` (`identity`, `manifest --normalize-root`, `events`, `text`) and `tests/check_prereg.py` — plus `tests/score_ledger.py` against the on-disk final ledgers.
Every machine-checkable claim below derives from re-executed commands in the scoring session, not from any run's self-report; a prior scorer's working notes existed for this wave but contained no reusable adjudications (see the wave section), so everything here is first-derivation.
Transcript tool timestamps are 2026-07-19T03:46–03:56Z (UTC), i.e. the evening of 2026-07-18 local; the wave is dated by dispatch.

## Transcript digests (sha256)

Piped through `shasum -a 256` in the scoring session; the `identity` subcommand's own digests agree byte-for-byte on all three:

```text
4ae856caac42ef7f39560461cebf8a05f1836613a6e5abec751fe8c91334fd24  s15-post-a.jsonl
3227e51e45a2e62cbf7892e0433f019bd86a996b16705a90cc0acf4e3d5c6f3d  s15-post-b.jsonl
9c47f9dd8bc1b9a0610f8ddc9cd4b19f95ccc73b100b89906bf1e5101aff83f9  s15-post-c.jsonl
```

## Identity counts vs harness-reported

```text
file: s15-post-a.jsonl
  sha256: 4ae856caac42ef7f39560461cebf8a05f1836613a6e5abec751fe8c91334fd24
  bytes: 433444  jsonl_lines: 57
  tool_use: 19  (Bash: 11, Read: 4, Write: 4)
  paired_results: error: 1, ok: 18
file: s15-post-b.jsonl
  sha256: 3227e51e45a2e62cbf7892e0433f019bd86a996b16705a90cc0acf4e3d5c6f3d
  bytes: 363882  jsonl_lines: 47
  tool_use: 14  (Bash: 10, Read: 3, Write: 1)
  paired_results: ok: 14
file: s15-post-c.jsonl
  sha256: 9c47f9dd8bc1b9a0610f8ddc9cd4b19f95ccc73b100b89906bf1e5101aff83f9
  bytes: 523109  jsonl_lines: 67
  tool_use: 21  (Bash: 9, Edit: 1, Read: 7, Write: 4)
  paired_results: error: 1, ok: 20
```

Harness-reported tool uses (19 / 14 / 21) match the identity counts exactly.
Token totals (102,762 / 94,222 / 121,366) are harness-reported per the suite's convention (the Ninth wave recorded that transcript usage fields do not reproduce harness totals under any simple sum; not re-derived here).
Arm a's one `error` result is ordinal 10 (a failed `import pandas` probe); arm c's is ordinal 9 (same probe) — neither is a fixture touch failure.

## Fixture digests

Recomputed with `shasum -a 256` at scoring time; the controller separately attested the digests unchanged at dispatch (that attestation is the controller's, not re-established here — what is established here is that the fixture on disk at scoring time is the one below, and that every fixture path in the manifests points into this directory):

```text
459122369ead9baf4c15447c672f0ea031e73ea247a28a77dc1bbbf40c45b4b7  activity.csv
10b31a2629b51bc2cd903737db377066613857b406830d9d9ef243d8a001d09c  incidents.csv
c60e2f5ddd08fdee6532ad7342e2a923b0fc7355a40bb927a40fea25f84b8236  PROBLEM.md
63cd5de5cbbe8012424f1f0638a83a5560a042d446b4c17dfa26c865b00c2fec  staffing.csv
```

## Manifests

TSV from `extract_evidence.py --normalize-root <repo> manifest`; columns: ordinal, timestamp, tool, result status, target.
Secrets scan over all three manifests: `grep -inE 'api[_-]?key|secret|passw|token[=:]|bearer|AKIA|ghp_|sk-[a-zA-Z0-9]'` returned no matches; the same pattern fires on a planted `api_key=abc123` line run in the same session.

### arm a (`s15-post-a.jsonl`)

```text
1	2026-07-19T03:46:07.082Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/SKILL.md
2	2026-07-19T03:46:14.401Z	Bash	ok	ls -la <REPO_ROOT>/hypothesis-driven-analysis/references/ && echo "---" && ls -la <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/
3	2026-07-19T03:46:16.704Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/PROBLEM.md
4	2026-07-19T03:46:17.065Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/references/ledger-template.md
5	2026-07-19T03:46:17.489Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/references/subagent-briefs.md
6	2026-07-19T03:46:21.650Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "=== incidents.csv head ===" && head -5 incidents.csv && echo "..." && wc -l incidents.csv && echo && echo "=== activity.csv head ===" && head -5 activity.csv && wc -l activity.csv && echo && echo "=== staffing.csv ===" && cat staffing.csv
7	2026-07-19T03:46:30.814Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "=== service_group values ===" && csvcut -c service_group incidents.csv 2>/dev/null | sort | uniq -c || (tail -n +2 incidents.csv | awk -F',' '{print $3}' | sort | uniq -c)\necho "=== workflow values ===" && tail -n +2 incidents.csv | awk -F',' '{print $6}' | sort | uniq -c\necho "=== severity values ===" && tail -n +2 incidents.csv | awk -F',' '{print $4}' | sort | uniq -c\necho "=== intake_channel values ===" && tail -n +2 incidents.csv | awk -F',' '{print $5}' | sort | uniq -c\necho "=== date range ===" && tail -n +2 incidents.csv | awk -F',' '{print $2}' | sort | head -3\ntail -n +2 incidents.csv | awk -F',' '{print $2}' | sort | tail -3\n
8	2026-07-19T03:46:36.385Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && tail -n +2 incidents.csv | awk -F',' '{print $3}' | sort | uniq -c
9	2026-07-19T03:46:50.855Z	Bash	ok	mkdir -p <SCRATCH>/s15-arms/a\ncd <SCRATCH>/s15-arms/a\npython3 --version\nwhich python3
10	2026-07-19T03:46:56.992Z	Bash	error	python3 -c "import pandas" 2>&1 | tail -5; python3 -c "import pandas; print(pandas.__version__)" 2>&1
11	2026-07-19T03:47:31.830Z	Write	ok	<SCRATCH>/s15-arms/a/orient.py
12	2026-07-19T03:47:34.223Z	Bash	ok	cd <SCRATCH>/s15-arms/a && python3 orient.py
13	2026-07-19T03:48:12.057Z	Write	ok	<SCRATCH>/s15-arms/a/analysis.py
14	2026-07-19T03:48:14.148Z	Bash	ok	cd <SCRATCH>/s15-arms/a && python3 analysis.py
15	2026-07-19T03:50:19.244Z	Write	ok	<SCRATCH>/s15-arms/a/analysis2.py
16	2026-07-19T03:50:21.634Z	Bash	ok	cd <SCRATCH>/s15-arms/a && python3 analysis2.py
17	2026-07-19T03:51:03.353Z	Bash	ok	cd <SCRATCH>/s15-arms/a && python3 - <<'EOF'\nimport csv, datetime, collections\nD = "<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/"\ndef parse(ts): return datetime.datetime.strptime(ts, "%Y-%m-%dT%H:%M:%SZ")\nincidents={}\nwith open(D+"incidents.csv") as f:\n    for row in csv.DictReader(f):\n        row["opened_at"]=parse(row["opened_at"]); incidents[row["incident_id"]]=row\nactivity={}\nwith open(D+"activity.csv") as f:\n    for row in csv.DictReader(f):\n        row["closed_at"]=parse(row["closed_at"])\n        for k in ["responder_minutes","handoffs","reopened_within_72h"]: row[k]=int(row[k])\n        activity[row["incident_id"]]=row\nmerged=[]\nfor iid,inc in incidents.items():\n    act=activity.get(iid)\n    r=dict(inc)\n    if act:\n        r.update(closed_at=act["closed_at"], responder_minutes=act["responder_minutes"],\n                 handoffs=act["handoffs"], reopened_within_72h=act["reopened_within_72h"], censored=False)\n    else:\n        r["censored"]=True\n    merged.append(r)\n\nprint("reopened_within_72h by severity x workflow (closed only)")\nfor sev in ["sev1","sev2","sev3"]:\n    for wf in ["manual","assist"]:\n        vals=[r["reopened_within_72h"] for r in merged if r["workflow"]==wf and r["severity"]==sev and not r["censored"]]\n        n=len(vals)\n        print(sev, wf, "n=",n, "rate=", sum(vals)/n if n else None)\n\nprint()\nprint("reopened_within_72h by intake_channel x workflow")\nfor ch in ["monitor","partner","customer"]:\n    for wf in ["manual","assist"]:\n        vals=[r["reopened_within_72h"] for r in merged if r["workflow"]==wf and r["intake_channel"]==ch and not r["censored"]]\n        n=len(vals)\n        print(ch, wf, "n=",n, "rate=", sum(vals)/n if n else None)\n\nprint()\nprint("intake_channel x severity distribution to check for another compositional confound")\nc=collections.Counter((r["intake_channel"],r["severity"]) for r in merged)\nfor k in sorted(c): print(k,c[k])\nEOF
18	2026-07-19T03:54:01.535Z	Bash	ok	cd <SCRATCH>/s15-arms/a && python3 - <<'EOF'\nimport csv, datetime, statistics\nD = "<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/"\nEXTRACT = datetime.datetime(2026,6,22,23,59,0)\ndef parse(ts): return datetime.datetime.strptime(ts, "%Y-%m-%dT%H:%M:%SZ")\nincidents={}\nwith open(D+"incidents.csv") as f:\n    for row in csv.DictReader(f):\n        row["opened_at"]=parse(row["opened_at"]); incidents[row["incident_id"]]=row\nactivity={}\nwith open(D+"activity.csv") as f:\n    for row in csv.DictReader(f):\n        row["closed_at"]=parse(row["closed_at"])\n        for k in ["responder_minutes","handoffs","reopened_within_72h"]: row[k]=int(row[k])\n        activity[row["incident_id"]]=row\n\nsev1_assist_closed_ttc=[]\nsev1_assist_open_age=[]\nfor iid,inc in incidents.items():\n    if inc["workflow"]=="assist" and inc["severity"]=="sev1":\n        act=activity.get(iid)\n        if act:\n            sev1_assist_closed_ttc.append((act["closed_at"]-inc["opened_at"]).total_seconds()/60)\n        else:\n            sev1_assist_open_age.append((EXTRACT-inc["opened_at"]).total_seconds()/60)\n\nprint("sev1 assist closed ttc (n=%d):"%len(sev1_assist_closed_ttc), sorted(sev1_assist_closed_ttc))\nprint("sev1 assist open age-so-far (n=%d, still counting):"%len(sev1_assist_open_age), sorted(sev1_assist_open_age))\nblended = sev1_assist_closed_ttc+sev1_assist_open_age\nprint("blended floor mean (treats still-open age as a lower bound on eventual ttc): %.1f min"%(sum(blended)/len(blended)))\nprint("vs manual sev1 mean 1071.6 min, all 28 closed")\n\nprint()\nprint("manual sev1 ttc max = 1274 (0.885 days); assist sev1 closed ttc min = %.0f (%.2f days)"%(min(sev1_assist_closed_ttc), min(sev1_assist_closed_ttc)/1440))\nprint("-> zero overlap confirmed: manual sev1 max < assist sev1 closed min")\n\n# overlap check sev2, sev3\ndef vals(sev, wf):\n    out=[]\n    for iid,inc in incidents.items():\n        if inc["workflow"]==wf and inc["severity"]==sev:\n            act=activity.get(iid)\n            if act: out.append((act["closed_at"]-inc["opened_at"]).total_seconds()/60)\n    return out\nfor sev in ["sev1","sev2","sev3"]:\n    m=vals(sev,"manual"); a=vals(sev,"assist")\n    print(sev,"manual range", min(m),max(m), "| assist range", min(a),max(a), "overlap:", max(m)>=min(a))\nEOF
19	2026-07-19T03:55:19.118Z	Write	ok	<SCRATCH>/s15-arms/a/ledger.md
```

### arm b (`s15-post-b.jsonl`)

```text
1	2026-07-19T03:46:14.539Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/SKILL.md
2	2026-07-19T03:46:24.943Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/references/ledger-template.md
3	2026-07-19T03:46:27.860Z	Bash	ok	ls -la <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "---" && wc -l <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/*.csv
4	2026-07-19T03:46:31.408Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/PROBLEM.md
5	2026-07-19T03:46:34.047Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "=== incidents.csv header + sample ===" && head -5 incidents.csv && echo "..." && echo "=== activity.csv header + sample ===" && head -5 activity.csv && echo "=== staffing.csv full ===" && cat staffing.csv
6	2026-07-19T03:46:42.755Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 -c "\nimport csv\nfrom collections import Counter, defaultdict\n\nwith open('incidents.csv') as f:\n    incidents = list(csv.DictReader(f))\nwith open('activity.csv') as f:\n    activity = list(csv.DictReader(f))\nwith open('staffing.csv') as f:\n    staffing = list(csv.DictReader(f))\n\nprint('incidents rows:', len(incidents))\nprint('activity rows:', len(activity))\nprint('staffing rows:', len(staffing))\n\nprint()\nprint('workflow counts:', Counter(i['workflow'] for i in incidents))\nprint('service_group counts:', Counter(i['service_group'] for i in incidents))\nprint('severity counts:', Counter(i['severity'] for i in incidents))\nprint('intake_channel counts:', Counter(i['intake_channel'] for i in incidents))\n\n# date range\nopens = sorted(i['opened_at'] for i in incidents)\nprint('open range:', opens[0], opens[-1])\n\n# workflow by date\nfrom datetime import datetime\nwf_by_date = defaultdict(Counter)\nfor i in incidents:\n    d = i['opened_at'][:10]\n    wf_by_date[d][i['workflow']] += 1\nfor d in sorted(wf_by_date):\n    print(d, dict(wf_by_date[d]))\n"\n
7	2026-07-19T03:46:49.985Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 -c "\nimport csv\nfrom collections import Counter, defaultdict\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nmissing = [iid for iid in incidents if iid not in activity]\nprint('missing activity (still open) count:', len(missing))\nfor iid in missing:\n    i = incidents[iid]\n    print(iid, i['opened_at'], i['service_group'], i['severity'], i['workflow'])\n"\n
8	2026-07-19T03:47:21.366Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nEXTRACT = parse("2026-06-22T23:59:00Z")\n\nrows = []\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    if act:\n        closed = parse(act['closed_at'])\n        ttc_min = (closed - opened).total_seconds()/60\n        rows.append(dict(iid=iid, workflow=inc['workflow'], sg=inc['service_group'],\n                          sev=inc['severity'], ch=inc['intake_channel'], opened=opened,\n                          closed=closed, ttc_min=ttc_min, resp_min=float(act['responder_minutes']),\n                          handoffs=int(act['handoffs']), reopened=int(act['reopened_within_72h']),\n                          status='closed'))\n    else:\n        age_min = (EXTRACT - opened).total_seconds()/60\n        rows.append(dict(iid=iid, workflow=inc['workflow'], sg=inc['service_group'],\n                          sev=inc['severity'], ch=inc['intake_channel'], opened=opened,\n                          closed=None, ttc_min=None, resp_min=None,\n                          handoffs=None, reopened=None, status='open', age_min=age_min))\n\nclosed_rows = [r for r in rows if r['status']=='closed']\nopen_rows = [r for r in rows if r['status']=='open']\n\nprint("=== Closed-only stats by workflow (what a naive 'median time to close' would use) ===")\nfor wf in ['manual','assist']:\n    vals = sorted(r['ttc_min'] for r in closed_rows if r['workflow']==wf)\n    n = len(vals)\n    med = stats.median(vals)\n    mean = stats.mean(vals)\n    print(f"{wf}: n={n}, median={med:.1f} min ({med/60:.2f}h), mean={mean:.1f} min ({mean/60:.2f}h), min={vals[0]:.0f}, max={vals[-1]:.0f}")\n\nprint()\nprint("=== Open incidents (right-censored), by workflow, with current age ===")\nfor wf in ['manual','assist']:\n    ors = [r for r in open_rows if r['workflow']==wf]\n    print(f"{wf}: n_open={len(ors)}")\n    for r in sorted(ors, key=lambda r:-r['age_min']):\n        print(f"   {r['iid']} sev={r['sev']} sg={r['sg']} opened={r['opened']} age_so_far={r['age_min']/60:.1f}h ({r['age_min']/1440:.1f}d)")\n\nprint()\nprint("=== max closed ttc (for reference, what's the longest an incident took to close in each arm) ===")\nfor wf in ['manual','assist']:\n    vals = sorted((r['ttc_min'] for r in closed_rows if r['workflow']==wf), reverse=True)\n    print(wf, 'top5 longest closed (hours):', [f"{v/60:.1f}" for v in vals[:5]])\nEOF\n
9	2026-07-19T03:47:50.933Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nrows = []\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    if act:\n        closed = parse(act['closed_at'])\n        ttc_min = (closed - opened).total_seconds()/60\n        rows.append(dict(iid=iid, workflow=inc['workflow'], sg=inc['service_group'],\n                          sev=inc['severity'], ch=inc['intake_channel'],\n                          ttc_min=ttc_min, resp_min=float(act['responder_minutes']),\n                          handoffs=int(act['handoffs']), reopened=int(act['reopened_within_72h'])))\n\nprint("=== severity mix by workflow (closed incidents) ===")\nfor wf in ['manual','assist']:\n    c = Counter(r['sev'] for r in rows if r['workflow']==wf)\n    print(wf, dict(c))\n\nprint()\nprint("=== ttc median (hours) by severity x workflow, closed only ===")\nfor sev in ['sev1','sev2','sev3']:\n    line = f"{sev}: "\n    for wf in ['manual','assist']:\n        vals = [r['ttc_min'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        if vals:\n            line += f"{wf} n={len(vals)} median={stats.median(vals)/60:.2f}h mean={stats.mean(vals)/60:.2f}h  "\n    print(line)\n\nprint()\nprint("=== responder_minutes median/mean by workflow, closed only ===")\nfor wf in ['manual','assist']:\n    vals = [r['resp_min'] for r in rows if r['workflow']==wf]\n    print(wf, 'n=',len(vals),'median=',stats.median(vals),'mean=',round(stats.mean(vals),1), 'total=', sum(vals))\n\nprint()\nprint("=== responder_minutes median/mean by severity x workflow, closed only ===")\nfor sev in ['sev1','sev2','sev3']:\n    line = f"{sev}: "\n    for wf in ['manual','assist']:\n        vals = [r['resp_min'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        if vals:\n            line += f"{wf} n={len(vals)} median={stats.median(vals):.1f} mean={stats.mean(vals):.1f}  "\n    print(line)\n\nprint()\nprint("=== handoffs mean by workflow ===")\nfor wf in ['manual','assist']:\n    vals = [r['handoffs'] for r in rows if r['workflow']==wf]\n    print(wf, 'mean handoffs=', round(stats.mean(vals),2))\n\nprint()\nprint("=== reopened_within_72h rate by workflow ===")\nfor wf in ['manual','assist']:\n    vals = [r['reopened'] for r in rows if r['workflow']==wf]\n    print(wf, 'n=',len(vals), 'reopen rate=', round(sum(vals)/len(vals),3))\n\nprint()\nprint("=== service_group x workflow closed counts ===")\nc = Counter((r['workflow'], r['sg']) for r in rows)\nprint(dict(c))\n\nprint()\nprint("=== intake_channel mix by workflow ===")\nfor wf in ['manual','assist']:\n    c = Counter(r['ch'] for r in rows if r['workflow']==wf)\n    print(wf, dict(c))\nEOF\n
10	2026-07-19T03:48:50.996Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\n# full severity x workflow counts (ALL incidents, closed + open)\nc = Counter((inc['workflow'], inc['severity']) for inc in incidents.values())\nprint("=== ALL incidents (closed+open) by workflow x severity ===")\nfor wf in ['manual','assist']:\n    print(wf, {sev: c[(wf,sev)] for sev in ['sev1','sev2','sev3']})\n\n# open counts by severity x workflow\nopen_ids = [iid for iid in incidents if iid not in activity]\noc = Counter((incidents[iid]['workflow'], incidents[iid]['severity']) for iid in open_ids)\nprint()\nprint("=== OPEN (uncensored/still-open) by workflow x severity ===")\nfor wf in ['manual','assist']:\n    print(wf, {sev: oc.get((wf,sev),0) for sev in ['sev1','sev2','sev3']})\n\n# fraction open, of total, by severity x workflow\nprint()\nprint("=== fraction still open, by workflow x severity ===")\nfor wf in ['manual','assist']:\n    for sev in ['sev1','sev2','sev3']:\n        tot = c[(wf,sev)]\n        op = oc.get((wf,sev),0)\n        print(f"{wf} {sev}: {op}/{tot} = {op/tot:.1%} open")\n\n# Standardized (reweighted) comparison: apply manual's severity mix as weights to assist's per-severity mean ttc\nprint()\nprint("=== Direct standardization: what would assist's overall mean ttc be if assist had manual's severity mix? (closed only, so still optimistic for assist since it drops assist's stuck sev1s) ===")\nrows=[]\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    if act:\n        opened = parse(inc['opened_at']); closed = parse(act['closed_at'])\n        rows.append(dict(workflow=inc['workflow'], sev=inc['severity'], ttc=(closed-opened).total_seconds()/60, resp=float(act['responder_minutes'])))\n\nmanual_mix = Counter(r['sev'] for r in rows if r['workflow']=='manual')\nmanual_n = sum(manual_mix.values())\nmanual_w = {k: v/manual_n for k,v in manual_mix.items()}\nprint("manual severity weights:", {k: round(v,3) for k,v in manual_w.items()})\n\nassist_sev_mean_ttc = {}\nassist_sev_mean_resp = {}\nfor sev in ['sev1','sev2','sev3']:\n    vals = [r['ttc'] for r in rows if r['workflow']=='assist' and r['sev']==sev]\n    assist_sev_mean_ttc[sev] = stats.mean(vals)\n    valsr = [r['resp'] for r in rows if r['workflow']=='assist' and r['sev']==sev]\n    assist_sev_mean_resp[sev] = stats.mean(valsr)\n\nstandardized_ttc = sum(assist_sev_mean_ttc[s]*manual_w[s] for s in manual_w)\nstandardized_resp = sum(assist_sev_mean_resp[s]*manual_w[s] for s in manual_w)\nmanual_mean_ttc = stats.mean([r['ttc'] for r in rows if r['workflow']=='manual'])\nmanual_mean_resp = stats.mean([r['resp'] for r in rows if r['workflow']=='manual'])\nprint(f"manual actual mean ttc: {manual_mean_ttc/60:.2f}h; assist mean ttc standardized to manual severity mix: {standardized_ttc/60:.2f}h")\nprint(f"manual actual mean resp_min: {manual_mean_resp:.1f}; assist mean resp_min standardized to manual severity mix: {standardized_resp:.1f}")\nEOF\n
11	2026-07-19T03:49:34.696Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nrows=[]\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    if act:\n        rows.append(dict(workflow=inc['workflow'], sev=inc['severity'], sg=inc['service_group'],\n                          handoffs=int(act['handoffs']), reopened=int(act['reopened_within_72h']),\n                          resp=float(act['responder_minutes'])))\n\nprint("=== handoffs mean by severity x workflow ===")\nfor sev in ['sev1','sev2','sev3']:\n    line=f"{sev}: "\n    for wf in ['manual','assist']:\n        vals=[r['handoffs'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        line += f"{wf} mean={stats.mean(vals):.2f} n={len(vals)}  "\n    print(line)\n\nprint()\nprint("=== reopened_within_72h rate by severity x workflow ===")\nfor sev in ['sev1','sev2','sev3']:\n    line=f"{sev}: "\n    for wf in ['manual','assist']:\n        vals=[r['reopened'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        line += f"{wf} rate={sum(vals)/len(vals):.3f} n={len(vals)}  "\n    print(line)\n\nprint()\nprint("=== service_group breakdown of ttc/resp (closed), by workflow ===")\nfor sg in ['identity','storage']:\n    for wf in ['manual','assist']:\n        vals=[r['resp'] for r in rows if r['workflow']==wf and r['sg']==sg]\n        print(sg, wf, 'n=',len(vals),'mean resp=', round(stats.mean(vals),1))\n\n# staffing check: interruption minutes averages by week\nprint()\nwith open('staffing.csv') as f:\n    staffing = list(csv.DictReader(f))\nmanual_days = [s for s in staffing if s['date'] <= '2026-06-07']\nassist_days = [s for s in staffing if s['date'] >= '2026-06-08']\nfor sg in ['identity','storage']:\n    m = [int(s['interruption_minutes']) for s in manual_days if s['service_group']==sg]\n    a = [int(s['interruption_minutes']) for s in assist_days if s['service_group']==sg]\n    print(f"{sg} interruption_minutes: manual week avg={stats.mean(m):.1f}, assist week avg={stats.mean(a):.1f}")\n    ar_m = [int(s['active_responders']) for s in manual_days if s['service_group']==sg]\n    ar_a = [int(s['active_responders']) for s in assist_days if s['service_group']==sg]\n    print(f"{sg} active_responders: manual avg={stats.mean(ar_m):.1f}, assist avg={stats.mean(ar_a):.1f}")\nEOF\n
12	2026-07-19T03:52:01.701Z	Bash	ok	mkdir -p <SCRATCH>/s15-arms/b && cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nrows=[]\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    d = inc['opened_at'][:10]\n    if act:\n        closed = parse(act['closed_at'])\n        rows.append(dict(iid=iid, date=d, workflow=inc['workflow'], sev=inc['severity'],\n                          ch=inc['intake_channel'], ttc=(closed-opened).total_seconds()/60,\n                          resp=float(act['responder_minutes']), status='closed'))\n    else:\n        rows.append(dict(iid=iid, date=d, workflow=inc['workflow'], sev=inc['severity'],\n                          ch=inc['intake_channel'], ttc=None, resp=None, status='open'))\n\nprint("=== HELD-OUT CHECK 1: day-by-day median ttc (closed only) + open-count, by date ===")\ndates = sorted(set(r['date'] for r in rows))\nfor d in dates:\n    day_rows = [r for r in rows if r['date']==d]\n    closed = [r['ttc'] for r in day_rows if r['status']=='closed']\n    n_open = sum(1 for r in day_rows if r['status']=='open')\n    wf = day_rows[0]['workflow']\n    sevmix = Counter(r['sev'] for r in day_rows)\n    med = stats.median(closed) if closed else float('nan')\n    print(f"{d} ({wf}): n_closed={len(closed)} median_ttc_h={med/60:.2f} n_open={n_open} sevmix={dict(sevmix)}")\n\nprint()\nprint("=== HELD-OUT CHECK 2: intake_channel x workflow, ttc median (closed only) ===")\nfor ch in ['monitor','partner','customer']:\n    line = f"{ch}: "\n    for wf in ['manual','assist']:\n        vals = [r['ttc'] for r in rows if r['workflow']==wf and r['ch']==ch and r['status']=='closed']\n        if vals:\n            line += f"{wf} n={len(vals)} median={stats.median(vals)/60:.2f}h  "\n    print(line)\n\nprint()\nprint("=== HELD-OUT CHECK 3: intake_channel x workflow x severity mix (composition check) ===")\nfor ch in ['monitor','partner','customer']:\n    for wf in ['manual','assist']:\n        c = Counter(r['sev'] for r in rows if r['workflow']==wf and r['ch']==ch)\n        print(ch, wf, dict(c))\nEOF\n
13	2026-07-19T03:52:38.500Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv, random\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\nrandom.seed(42)\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nEXTRACT = parse("2026-06-22T23:59:00Z")\n\nclosed = defaultdict(list)  # (workflow,sev) -> list of ttc_min\nopen_ages = defaultdict(list)\nsev1_manual_closed = []\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    wf, sev = inc['workflow'], inc['severity']\n    if act:\n        closed_t = parse(act['closed_at'])\n        ttc = (closed_t-opened).total_seconds()/60\n        closed[(wf,sev)].append(ttc)\n    else:\n        age = (EXTRACT-opened).total_seconds()/60\n        open_ages[(wf,sev)].append(age)\n\nmanual_mix_n = Counter(inc['severity'] for inc in incidents.values() if inc['workflow']=='manual')\ntot = sum(manual_mix_n.values())\nmanual_w = {k: v/tot for k,v in manual_mix_n.items()}\n\ndef standardized_mean(sample):\n    # sample: dict (wf,sev)->list, compute assist standardized mean ttc using manual_w\n    num = 0\n    for sev,w in manual_w.items():\n        vals = sample[('assist',sev)]\n        num += w*stats.mean(vals)\n    return num\n\nmanual_actual_mean = stats.mean(closed[('manual','sev1')]+closed[('manual','sev2')]+closed[('manual','sev3')])\n\n# Bootstrap: resample within each (wf,sev) closed group with replacement, recompute standardized mean for assist,\n# and manual actual mean, track difference\nN=3000\ndiffs=[]\nfor _ in range(N):\n    boot = {}\n    for sev in ['sev1','sev2','sev3']:\n        vals = closed[('assist',sev)]\n        boot[('assist',sev)] = [random.choice(vals) for _ in vals]\n    std_mean = standardized_mean(boot)\n    manual_vals = closed[('manual','sev1')]+closed[('manual','sev2')]+closed[('manual','sev3')]\n    boot_manual = [random.choice(manual_vals) for _ in manual_vals]\n    manual_mean_b = stats.mean(boot_manual)\n    diffs.append(std_mean - manual_mean_b)\n\ndiffs.sort()\nlo = diffs[int(0.025*N)]\nhi = diffs[int(0.975*N)]\npoint = standardized_mean(closed) - manual_actual_mean\nprint(f"Standardized assist mean ttc minus manual actual mean ttc (minutes): point={point:.1f}, 95% bootstrap CI=({lo:.1f}, {hi:.1f})")\nprint(f"in hours: point={point/60:.2f}h, CI=({lo/60:.2f}h, {hi/60:.2f}h)")\nprint("Does CI include 0 (no difference)?", lo<=0<=hi)\nprint("Does CI include the 'claimed improvement' direction (negative, i.e. assist faster)?", hi<0)\n\nprint()\nprint("=== Deterministic bound check: sev1 manual max CLOSED ttc vs sev1 assist OPEN current ages ===")\nsev1_manual_max = max(closed[('manual','sev1')])\nsev1_assist_open_ages = sorted(open_ages[('assist','sev1')])\nprint(f"manual sev1 max closed ttc: {sev1_manual_max/60:.1f}h")\nprint(f"assist sev1 currently-open ages (h): {[round(a/60,1) for a in sev1_assist_open_ages]}")\nprint(f"even the LEAST mature open assist sev1 incident is already {min(sev1_assist_open_ages)/60:.1f}h old, "\n      f"vs manual's sev1 max closed of {sev1_manual_max/60:.1f}h -> ratio {min(sev1_assist_open_ages)/sev1_manual_max:.1f}x")\n\nsev1_assist_closed_max = max(closed[('assist','sev1')])\nprint(f"assist sev1 max CLOSED ttc (for reference): {sev1_assist_closed_max/60:.1f}h")\nEOF\n
14	2026-07-19T03:54:09.209Z	Write	ok	<SCRATCH>/s15-arms/b/ledger.md
```

### arm c (`s15-post-c.jsonl`)

```text
1	2026-07-19T03:46:20.429Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/SKILL.md
2	2026-07-19T03:46:28.265Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/references/ledger-template.md
3	2026-07-19T03:46:30.804Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/references/subagent-briefs.md
4	2026-07-19T03:46:33.782Z	Bash	ok	ls -la <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "---" && wc -l <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/*.csv
5	2026-07-19T03:46:39.242Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/PROBLEM.md
6	2026-07-19T03:46:41.180Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/incidents.csv
7	2026-07-19T03:46:45.360Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/activity.csv
8	2026-07-19T03:46:49.790Z	Read	ok	<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/staffing.csv
9	2026-07-19T03:46:58.462Z	Bash	error	cd <SCRATCH>/s15-arms/c && mkdir -p analysis && python3 -c "import pandas, numpy; print('ok', pandas.__version__, numpy.__version__)"
10	2026-07-19T03:47:05.037Z	Bash	ok	which uv python3; python3 --version; uv --version 2>&1
11	2026-07-19T03:47:18.994Z	Write	ok	<SCRATCH>/s15-arms/c/analysis/load.py
12	2026-07-19T03:47:21.017Z	Bash	ok	cd <SCRATCH>/s15-arms/c && python3 analysis/load.py
13	2026-07-19T03:48:16.701Z	Bash	ok	cd <SCRATCH>/s15-arms/c && wc -l <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/*.csv
14	2026-07-19T03:48:35.472Z	Write	ok	<SCRATCH>/s15-arms/c/analysis/orient.py
15	2026-07-19T03:48:38.356Z	Bash	ok	cd <SCRATCH>/s15-arms/c && python3 analysis/orient.py
16	2026-07-19T03:51:27.774Z	Write	ok	<SCRATCH>/s15-arms/c/ledger.md
17	2026-07-19T03:52:05.828Z	Write	ok	<SCRATCH>/s15-arms/c/analysis/tests.py
18	2026-07-19T03:52:08.429Z	Bash	ok	cd <SCRATCH>/s15-arms/c && python3 analysis/tests.py
19	2026-07-19T03:54:28.815Z	Bash	ok	cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout && grep -E "^INC0211,|^INC0044," incidents.csv && grep -E "^INC0044," activity.csv && echo "---sev1 assist rows for manual cross-check---" && awk -F, '$4=="sev1"' incidents.csv | grep assist | head -20
20	2026-07-19T03:54:46.004Z	Bash	ok	cd <SCRATCH>/s15-arms/c && python3 -c "\nfrom math import comb\n# Fisher exact, one-sided: P(X>=10) among assist given 11 total open incidents drawn from 420 (210/210 split),\n# under null that open-status is independent of workflow (hypergeometric: 210 assist / 420 total, 11 'open' draws)\nN=420; K=210; n=11; k_obs=10\ndef hyper(k):\n    return comb(K,k)*comb(N-K,n-k)/comb(N,n)\np_ge = sum(hyper(k) for k in range(k_obs, min(n,K)+1))\nprint('P(>=10 of 11 opens land in assist | independent of workflow) =', p_ge)\n\n# sev1-only: 42 sev1 total (28 manual/14 assist), 5 open among assist's 14, 0 among manual's 28\n# hypergeometric: population 42 sev1 incidents, 14 'assist' (successes), draw 5 open incidents, want P(all 5 are assist)\nN2=42; K2=14; n2=5; k2=5\np_sev1 = comb(K2,k2)*comb(N2-K2,n2-k2)/comb(N2,n2)\nprint('P(all 5 open sev1 incidents land in the 14 assist sev1 | independent of workflow) =', p_sev1)\n"\n
21	2026-07-19T03:55:44.082Z	Edit	ok	<SCRATCH>/s15-arms/c/ledger.md
```

## check_prereg.py outputs (verbatim)

Command per arm: `uv run tests/check_prereg.py --ledger-pattern 'ledger\.md' --data-pattern 's15-assist-rollout' <manifest>`.

### arm a

```text
PREREG_WRITE: ordinal 19 (Write -> <SCRATCH>/s15-arms/a/ledger.md)
CONTENT-CHECK REQUIRED: confirm from the events stream (extract_evidence.py events <transcript> --ordinal 19) that this write already carries the hypothesis table and predictions — a placeholder written early and filled after analysis is a reconstruction, not a preregistration.
CLASSIFY: 7 tool_use(s) precede the ledger write; classify each as orientation or analysis in the evidence artifact:
  - ordinal 2: Bash [ok] ls -la <REPO_ROOT>/hypothesis-driven-analysis/references/ && echo "---" && ls -la <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/
  - ordinal 3: Read [ok] <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/PROBLEM.md
  - ordinal 6: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "=== incidents.csv head ===" && head -5 incidents.csv && echo "..." && wc -l incidents.csv && echo && echo "=== activity.csv head ===" && head -5 activity.csv && wc -l activity.csv && echo && echo "=== staffing.csv ===" && cat staffing.csv
  - ordinal 7: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "=== service_group values ===" && csvcut -c service_group incidents.csv 2>/dev/null | sort | uniq -c || (tail -n +2 incidents.csv | awk -F',' '{print $3}' | sort | uniq -c)\necho "=== workflow values ===" && tail -n +2 incidents.csv | awk -F',' '{print $6}' | sort | uniq -c\necho "=== severity values ===" && tail -n +2 incidents.csv | awk -F',' '{print $4}' | sort | uniq -c\necho "=== intake_channel values ===" && tail -n +2 incidents.csv | awk -F',' '{print $5}' | sort | uniq -c\necho "=== date range ===" && tail -n +2 incidents.csv | awk -F',' '{print $2}' | sort | head -3\ntail -n +2 incidents.csv | awk -F',' '{print $2}' | sort | tail -3\n
  - ordinal 8: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && tail -n +2 incidents.csv | awk -F',' '{print $3}' | sort | uniq -c
  - ordinal 17: Bash [ok] cd <SCRATCH>/s15-arms/a && python3 - <<'EOF'\nimport csv, datetime, collections\nD = "<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/"\ndef parse(ts): return datetime.datetime.strptime(ts, "%Y-%m-%dT%H:%M:%SZ")\nincidents={}\nwith open(D+"incidents.csv") as f:\n    for row in csv.DictReader(f):\n        row["opened_at"]=parse(row["opened_at"]); incidents[row["incident_id"]]=row\nactivity={}\nwith open(D+"activity.csv") as f:\n    for row in csv.DictReader(f):\n        row["closed_at"]=parse(row["closed_at"])\n        for k in ["responder_minutes","handoffs","reopened_within_72h"]: row[k]=int(row[k])\n        activity[row["incident_id"]]=row\nmerged=[]\nfor iid,inc in incidents.items():\n    act=activity.get(iid)\n    r=dict(inc)\n    if act:\n        r.update(closed_at=act["closed_at"], responder_minutes=act["responder_minutes"],\n                 handoffs=act["handoffs"], reopened_within_72h=act["reopened_within_72h"], censored=False)\n    else:\n        r["censored"]=True\n    merged.append(r)\n\nprint("reopened_within_72h by severity x workflow (closed only)")\nfor sev in ["sev1","sev2","sev3"]:\n    for wf in ["manual","assist"]:\n        vals=[r["reopened_within_72h"] for r in merged if r["workflow"]==wf and r["severity"]==sev and not r["censored"]]\n        n=len(vals)\n        print(sev, wf, "n=",n, "rate=", sum(vals)/n if n else None)\n\nprint()\nprint("reopened_within_72h by intake_channel x workflow")\nfor ch in ["monitor","partner","customer"]:\n    for wf in ["manual","assist"]:\n        vals=[r["reopened_within_72h"] for r in merged if r["workflow"]==wf and r["intake_channel"]==ch and not r["censored"]]\n        n=len(vals)\n        print(ch, wf, "n=",n, "rate=", sum(vals)/n if n else None)\n\nprint()\nprint("intake_channel x severity distribution to check for another compositional confound")\nc=collections.Counter((r["intake_channel"],r["severity"]) for r in merged)\nfor k in sorted(c): print(k,c[k])\nEOF
  - ordinal 18: Bash [ok] cd <SCRATCH>/s15-arms/a && python3 - <<'EOF'\nimport csv, datetime, statistics\nD = "<REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/"\nEXTRACT = datetime.datetime(2026,6,22,23,59,0)\ndef parse(ts): return datetime.datetime.strptime(ts, "%Y-%m-%dT%H:%M:%SZ")\nincidents={}\nwith open(D+"incidents.csv") as f:\n    for row in csv.DictReader(f):\n        row["opened_at"]=parse(row["opened_at"]); incidents[row["incident_id"]]=row\nactivity={}\nwith open(D+"activity.csv") as f:\n    for row in csv.DictReader(f):\n        row["closed_at"]=parse(row["closed_at"])\n        for k in ["responder_minutes","handoffs","reopened_within_72h"]: row[k]=int(row[k])\n        activity[row["incident_id"]]=row\n\nsev1_assist_closed_ttc=[]\nsev1_assist_open_age=[]\nfor iid,inc in incidents.items():\n    if inc["workflow"]=="assist" and inc["severity"]=="sev1":\n        act=activity.get(iid)\n        if act:\n            sev1_assist_closed_ttc.append((act["closed_at"]-inc["opened_at"]).total_seconds()/60)\n        else:\n            sev1_assist_open_age.append((EXTRACT-inc["opened_at"]).total_seconds()/60)\n\nprint("sev1 assist closed ttc (n=%d):"%len(sev1_assist_closed_ttc), sorted(sev1_assist_closed_ttc))\nprint("sev1 assist open age-so-far (n=%d, still counting):"%len(sev1_assist_open_age), sorted(sev1_assist_open_age))\nblended = sev1_assist_closed_ttc+sev1_assist_open_age\nprint("blended floor mean (treats still-open age as a lower bound on eventual ttc): %.1f min"%(sum(blended)/len(blended)))\nprint("vs manual sev1 mean 1071.6 min, all 28 closed")\n\nprint()\nprint("manual sev1 ttc max = 1274 (0.885 days); assist sev1 closed ttc min = %.0f (%.2f days)"%(min(sev1_assist_closed_ttc), min(sev1_assist_closed_ttc)/1440))\nprint("-> zero overlap confirmed: manual sev1 max < assist sev1 closed min")\n\n# overlap check sev2, sev3\ndef vals(sev, wf):\n    out=[]\n    for iid,inc in incidents.items():\n        if inc["workflow"]==wf and inc["severity"]==sev:\n            act=activity.get(iid)\n            if act: out.append((act["closed_at"]-inc["opened_at"]).total_seconds()/60)\n    return out\nfor sev in ["sev1","sev2","sev3"]:\n    m=vals(sev,"manual"); a=vals(sev,"assist")\n    print(sev,"manual range", min(m),max(m), "| assist range", min(a),max(a), "overlap:", max(m)>=min(a))\nEOF
exit=1
```

### arm b

```text
PREREG_WRITE: ordinal 14 (Write -> <SCRATCH>/s15-arms/b/ledger.md)
CONTENT-CHECK REQUIRED: confirm from the events stream (extract_evidence.py events <transcript> --ordinal 14) that this write already carries the hypothesis table and predictions — a placeholder written early and filled after analysis is a reconstruction, not a preregistration.
CLASSIFY: 11 tool_use(s) precede the ledger write; classify each as orientation or analysis in the evidence artifact:
  - ordinal 3: Bash [ok] ls -la <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "---" && wc -l <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/*.csv
  - ordinal 4: Read [ok] <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/PROBLEM.md
  - ordinal 5: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "=== incidents.csv header + sample ===" && head -5 incidents.csv && echo "..." && echo "=== activity.csv header + sample ===" && head -5 activity.csv && echo "=== staffing.csv full ===" && cat staffing.csv
  - ordinal 6: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 -c "\nimport csv\nfrom collections import Counter, defaultdict\n\nwith open('incidents.csv') as f:\n    incidents = list(csv.DictReader(f))\nwith open('activity.csv') as f:\n    activity = list(csv.DictReader(f))\nwith open('staffing.csv') as f:\n    staffing = list(csv.DictReader(f))\n\nprint('incidents rows:', len(incidents))\nprint('activity rows:', len(activity))\nprint('staffing rows:', len(staffing))\n\nprint()\nprint('workflow counts:', Counter(i['workflow'] for i in incidents))\nprint('service_group counts:', Counter(i['service_group'] for i in incidents))\nprint('severity counts:', Counter(i['severity'] for i in incidents))\nprint('intake_channel counts:', Counter(i['intake_channel'] for i in incidents))\n\n# date range\nopens = sorted(i['opened_at'] for i in incidents)\nprint('open range:', opens[0], opens[-1])\n\n# workflow by date\nfrom datetime import datetime\nwf_by_date = defaultdict(Counter)\nfor i in incidents:\n    d = i['opened_at'][:10]\n    wf_by_date[d][i['workflow']] += 1\nfor d in sorted(wf_by_date):\n    print(d, dict(wf_by_date[d]))\n"\n
  - ordinal 7: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 -c "\nimport csv\nfrom collections import Counter, defaultdict\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nmissing = [iid for iid in incidents if iid not in activity]\nprint('missing activity (still open) count:', len(missing))\nfor iid in missing:\n    i = incidents[iid]\n    print(iid, i['opened_at'], i['service_group'], i['severity'], i['workflow'])\n"\n
  - ordinal 8: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nEXTRACT = parse("2026-06-22T23:59:00Z")\n\nrows = []\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    if act:\n        closed = parse(act['closed_at'])\n        ttc_min = (closed - opened).total_seconds()/60\n        rows.append(dict(iid=iid, workflow=inc['workflow'], sg=inc['service_group'],\n                          sev=inc['severity'], ch=inc['intake_channel'], opened=opened,\n                          closed=closed, ttc_min=ttc_min, resp_min=float(act['responder_minutes']),\n                          handoffs=int(act['handoffs']), reopened=int(act['reopened_within_72h']),\n                          status='closed'))\n    else:\n        age_min = (EXTRACT - opened).total_seconds()/60\n        rows.append(dict(iid=iid, workflow=inc['workflow'], sg=inc['service_group'],\n                          sev=inc['severity'], ch=inc['intake_channel'], opened=opened,\n                          closed=None, ttc_min=None, resp_min=None,\n                          handoffs=None, reopened=None, status='open', age_min=age_min))\n\nclosed_rows = [r for r in rows if r['status']=='closed']\nopen_rows = [r for r in rows if r['status']=='open']\n\nprint("=== Closed-only stats by workflow (what a naive 'median time to close' would use) ===")\nfor wf in ['manual','assist']:\n    vals = sorted(r['ttc_min'] for r in closed_rows if r['workflow']==wf)\n    n = len(vals)\n    med = stats.median(vals)\n    mean = stats.mean(vals)\n    print(f"{wf}: n={n}, median={med:.1f} min ({med/60:.2f}h), mean={mean:.1f} min ({mean/60:.2f}h), min={vals[0]:.0f}, max={vals[-1]:.0f}")\n\nprint()\nprint("=== Open incidents (right-censored), by workflow, with current age ===")\nfor wf in ['manual','assist']:\n    ors = [r for r in open_rows if r['workflow']==wf]\n    print(f"{wf}: n_open={len(ors)}")\n    for r in sorted(ors, key=lambda r:-r['age_min']):\n        print(f"   {r['iid']} sev={r['sev']} sg={r['sg']} opened={r['opened']} age_so_far={r['age_min']/60:.1f}h ({r['age_min']/1440:.1f}d)")\n\nprint()\nprint("=== max closed ttc (for reference, what's the longest an incident took to close in each arm) ===")\nfor wf in ['manual','assist']:\n    vals = sorted((r['ttc_min'] for r in closed_rows if r['workflow']==wf), reverse=True)\n    print(wf, 'top5 longest closed (hours):', [f"{v/60:.1f}" for v in vals[:5]])\nEOF\n
  - ordinal 9: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nrows = []\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    if act:\n        closed = parse(act['closed_at'])\n        ttc_min = (closed - opened).total_seconds()/60\n        rows.append(dict(iid=iid, workflow=inc['workflow'], sg=inc['service_group'],\n                          sev=inc['severity'], ch=inc['intake_channel'],\n                          ttc_min=ttc_min, resp_min=float(act['responder_minutes']),\n                          handoffs=int(act['handoffs']), reopened=int(act['reopened_within_72h'])))\n\nprint("=== severity mix by workflow (closed incidents) ===")\nfor wf in ['manual','assist']:\n    c = Counter(r['sev'] for r in rows if r['workflow']==wf)\n    print(wf, dict(c))\n\nprint()\nprint("=== ttc median (hours) by severity x workflow, closed only ===")\nfor sev in ['sev1','sev2','sev3']:\n    line = f"{sev}: "\n    for wf in ['manual','assist']:\n        vals = [r['ttc_min'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        if vals:\n            line += f"{wf} n={len(vals)} median={stats.median(vals)/60:.2f}h mean={stats.mean(vals)/60:.2f}h  "\n    print(line)\n\nprint()\nprint("=== responder_minutes median/mean by workflow, closed only ===")\nfor wf in ['manual','assist']:\n    vals = [r['resp_min'] for r in rows if r['workflow']==wf]\n    print(wf, 'n=',len(vals),'median=',stats.median(vals),'mean=',round(stats.mean(vals),1), 'total=', sum(vals))\n\nprint()\nprint("=== responder_minutes median/mean by severity x workflow, closed only ===")\nfor sev in ['sev1','sev2','sev3']:\n    line = f"{sev}: "\n    for wf in ['manual','assist']:\n        vals = [r['resp_min'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        if vals:\n            line += f"{wf} n={len(vals)} median={stats.median(vals):.1f} mean={stats.mean(vals):.1f}  "\n    print(line)\n\nprint()\nprint("=== handoffs mean by workflow ===")\nfor wf in ['manual','assist']:\n    vals = [r['handoffs'] for r in rows if r['workflow']==wf]\n    print(wf, 'mean handoffs=', round(stats.mean(vals),2))\n\nprint()\nprint("=== reopened_within_72h rate by workflow ===")\nfor wf in ['manual','assist']:\n    vals = [r['reopened'] for r in rows if r['workflow']==wf]\n    print(wf, 'n=',len(vals), 'reopen rate=', round(sum(vals)/len(vals),3))\n\nprint()\nprint("=== service_group x workflow closed counts ===")\nc = Counter((r['workflow'], r['sg']) for r in rows)\nprint(dict(c))\n\nprint()\nprint("=== intake_channel mix by workflow ===")\nfor wf in ['manual','assist']:\n    c = Counter(r['ch'] for r in rows if r['workflow']==wf)\n    print(wf, dict(c))\nEOF\n
  - ordinal 10: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\n# full severity x workflow counts (ALL incidents, closed + open)\nc = Counter((inc['workflow'], inc['severity']) for inc in incidents.values())\nprint("=== ALL incidents (closed+open) by workflow x severity ===")\nfor wf in ['manual','assist']:\n    print(wf, {sev: c[(wf,sev)] for sev in ['sev1','sev2','sev3']})\n\n# open counts by severity x workflow\nopen_ids = [iid for iid in incidents if iid not in activity]\noc = Counter((incidents[iid]['workflow'], incidents[iid]['severity']) for iid in open_ids)\nprint()\nprint("=== OPEN (uncensored/still-open) by workflow x severity ===")\nfor wf in ['manual','assist']:\n    print(wf, {sev: oc.get((wf,sev),0) for sev in ['sev1','sev2','sev3']})\n\n# fraction open, of total, by severity x workflow\nprint()\nprint("=== fraction still open, by workflow x severity ===")\nfor wf in ['manual','assist']:\n    for sev in ['sev1','sev2','sev3']:\n        tot = c[(wf,sev)]\n        op = oc.get((wf,sev),0)\n        print(f"{wf} {sev}: {op}/{tot} = {op/tot:.1%} open")\n\n# Standardized (reweighted) comparison: apply manual's severity mix as weights to assist's per-severity mean ttc\nprint()\nprint("=== Direct standardization: what would assist's overall mean ttc be if assist had manual's severity mix? (closed only, so still optimistic for assist since it drops assist's stuck sev1s) ===")\nrows=[]\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    if act:\n        opened = parse(inc['opened_at']); closed = parse(act['closed_at'])\n        rows.append(dict(workflow=inc['workflow'], sev=inc['severity'], ttc=(closed-opened).total_seconds()/60, resp=float(act['responder_minutes'])))\n\nmanual_mix = Counter(r['sev'] for r in rows if r['workflow']=='manual')\nmanual_n = sum(manual_mix.values())\nmanual_w = {k: v/manual_n for k,v in manual_mix.items()}\nprint("manual severity weights:", {k: round(v,3) for k,v in manual_w.items()})\n\nassist_sev_mean_ttc = {}\nassist_sev_mean_resp = {}\nfor sev in ['sev1','sev2','sev3']:\n    vals = [r['ttc'] for r in rows if r['workflow']=='assist' and r['sev']==sev]\n    assist_sev_mean_ttc[sev] = stats.mean(vals)\n    valsr = [r['resp'] for r in rows if r['workflow']=='assist' and r['sev']==sev]\n    assist_sev_mean_resp[sev] = stats.mean(valsr)\n\nstandardized_ttc = sum(assist_sev_mean_ttc[s]*manual_w[s] for s in manual_w)\nstandardized_resp = sum(assist_sev_mean_resp[s]*manual_w[s] for s in manual_w)\nmanual_mean_ttc = stats.mean([r['ttc'] for r in rows if r['workflow']=='manual'])\nmanual_mean_resp = stats.mean([r['resp'] for r in rows if r['workflow']=='manual'])\nprint(f"manual actual mean ttc: {manual_mean_ttc/60:.2f}h; assist mean ttc standardized to manual severity mix: {standardized_ttc/60:.2f}h")\nprint(f"manual actual mean resp_min: {manual_mean_resp:.1f}; assist mean resp_min standardized to manual severity mix: {standardized_resp:.1f}")\nEOF\n
  - ordinal 11: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nrows=[]\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    if act:\n        rows.append(dict(workflow=inc['workflow'], sev=inc['severity'], sg=inc['service_group'],\n                          handoffs=int(act['handoffs']), reopened=int(act['reopened_within_72h']),\n                          resp=float(act['responder_minutes'])))\n\nprint("=== handoffs mean by severity x workflow ===")\nfor sev in ['sev1','sev2','sev3']:\n    line=f"{sev}: "\n    for wf in ['manual','assist']:\n        vals=[r['handoffs'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        line += f"{wf} mean={stats.mean(vals):.2f} n={len(vals)}  "\n    print(line)\n\nprint()\nprint("=== reopened_within_72h rate by severity x workflow ===")\nfor sev in ['sev1','sev2','sev3']:\n    line=f"{sev}: "\n    for wf in ['manual','assist']:\n        vals=[r['reopened'] for r in rows if r['workflow']==wf and r['sev']==sev]\n        line += f"{wf} rate={sum(vals)/len(vals):.3f} n={len(vals)}  "\n    print(line)\n\nprint()\nprint("=== service_group breakdown of ttc/resp (closed), by workflow ===")\nfor sg in ['identity','storage']:\n    for wf in ['manual','assist']:\n        vals=[r['resp'] for r in rows if r['workflow']==wf and r['sg']==sg]\n        print(sg, wf, 'n=',len(vals),'mean resp=', round(stats.mean(vals),1))\n\n# staffing check: interruption minutes averages by week\nprint()\nwith open('staffing.csv') as f:\n    staffing = list(csv.DictReader(f))\nmanual_days = [s for s in staffing if s['date'] <= '2026-06-07']\nassist_days = [s for s in staffing if s['date'] >= '2026-06-08']\nfor sg in ['identity','storage']:\n    m = [int(s['interruption_minutes']) for s in manual_days if s['service_group']==sg]\n    a = [int(s['interruption_minutes']) for s in assist_days if s['service_group']==sg]\n    print(f"{sg} interruption_minutes: manual week avg={stats.mean(m):.1f}, assist week avg={stats.mean(a):.1f}")\n    ar_m = [int(s['active_responders']) for s in manual_days if s['service_group']==sg]\n    ar_a = [int(s['active_responders']) for s in assist_days if s['service_group']==sg]\n    print(f"{sg} active_responders: manual avg={stats.mean(ar_m):.1f}, assist avg={stats.mean(ar_a):.1f}")\nEOF\n
  - ordinal 12: Bash [ok] mkdir -p <SCRATCH>/s15-arms/b && cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nrows=[]\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    d = inc['opened_at'][:10]\n    if act:\n        closed = parse(act['closed_at'])\n        rows.append(dict(iid=iid, date=d, workflow=inc['workflow'], sev=inc['severity'],\n                          ch=inc['intake_channel'], ttc=(closed-opened).total_seconds()/60,\n                          resp=float(act['responder_minutes']), status='closed'))\n    else:\n        rows.append(dict(iid=iid, date=d, workflow=inc['workflow'], sev=inc['severity'],\n                          ch=inc['intake_channel'], ttc=None, resp=None, status='open'))\n\nprint("=== HELD-OUT CHECK 1: day-by-day median ttc (closed only) + open-count, by date ===")\ndates = sorted(set(r['date'] for r in rows))\nfor d in dates:\n    day_rows = [r for r in rows if r['date']==d]\n    closed = [r['ttc'] for r in day_rows if r['status']=='closed']\n    n_open = sum(1 for r in day_rows if r['status']=='open')\n    wf = day_rows[0]['workflow']\n    sevmix = Counter(r['sev'] for r in day_rows)\n    med = stats.median(closed) if closed else float('nan')\n    print(f"{d} ({wf}): n_closed={len(closed)} median_ttc_h={med/60:.2f} n_open={n_open} sevmix={dict(sevmix)}")\n\nprint()\nprint("=== HELD-OUT CHECK 2: intake_channel x workflow, ttc median (closed only) ===")\nfor ch in ['monitor','partner','customer']:\n    line = f"{ch}: "\n    for wf in ['manual','assist']:\n        vals = [r['ttc'] for r in rows if r['workflow']==wf and r['ch']==ch and r['status']=='closed']\n        if vals:\n            line += f"{wf} n={len(vals)} median={stats.median(vals)/60:.2f}h  "\n    print(line)\n\nprint()\nprint("=== HELD-OUT CHECK 3: intake_channel x workflow x severity mix (composition check) ===")\nfor ch in ['monitor','partner','customer']:\n    for wf in ['manual','assist']:\n        c = Counter(r['sev'] for r in rows if r['workflow']==wf and r['ch']==ch)\n        print(ch, wf, dict(c))\nEOF\n
  - ordinal 13: Bash [ok] cd <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && python3 << 'EOF'\nimport csv, random\nfrom datetime import datetime\nfrom collections import Counter, defaultdict\nimport statistics as stats\n\nrandom.seed(42)\n\ndef parse(t):\n    return datetime.strptime(t, "%Y-%m-%dT%H:%M:%SZ")\n\nwith open('incidents.csv') as f:\n    incidents = {i['incident_id']: i for i in csv.DictReader(f)}\nwith open('activity.csv') as f:\n    activity = {a['incident_id']: a for a in csv.DictReader(f)}\n\nEXTRACT = parse("2026-06-22T23:59:00Z")\n\nclosed = defaultdict(list)  # (workflow,sev) -> list of ttc_min\nopen_ages = defaultdict(list)\nsev1_manual_closed = []\nfor iid, inc in incidents.items():\n    act = activity.get(iid)\n    opened = parse(inc['opened_at'])\n    wf, sev = inc['workflow'], inc['severity']\n    if act:\n        closed_t = parse(act['closed_at'])\n        ttc = (closed_t-opened).total_seconds()/60\n        closed[(wf,sev)].append(ttc)\n    else:\n        age = (EXTRACT-opened).total_seconds()/60\n        open_ages[(wf,sev)].append(age)\n\nmanual_mix_n = Counter(inc['severity'] for inc in incidents.values() if inc['workflow']=='manual')\ntot = sum(manual_mix_n.values())\nmanual_w = {k: v/tot for k,v in manual_mix_n.items()}\n\ndef standardized_mean(sample):\n    # sample: dict (wf,sev)->list, compute assist standardized mean ttc using manual_w\n    num = 0\n    for sev,w in manual_w.items():\n        vals = sample[('assist',sev)]\n        num += w*stats.mean(vals)\n    return num\n\nmanual_actual_mean = stats.mean(closed[('manual','sev1')]+closed[('manual','sev2')]+closed[('manual','sev3')])\n\n# Bootstrap: resample within each (wf,sev) closed group with replacement, recompute standardized mean for assist,\n# and manual actual mean, track difference\nN=3000\ndiffs=[]\nfor _ in range(N):\n    boot = {}\n    for sev in ['sev1','sev2','sev3']:\n        vals = closed[('assist',sev)]\n        boot[('assist',sev)] = [random.choice(vals) for _ in vals]\n    std_mean = standardized_mean(boot)\n    manual_vals = closed[('manual','sev1')]+closed[('manual','sev2')]+closed[('manual','sev3')]\n    boot_manual = [random.choice(manual_vals) for _ in manual_vals]\n    manual_mean_b = stats.mean(boot_manual)\n    diffs.append(std_mean - manual_mean_b)\n\ndiffs.sort()\nlo = diffs[int(0.025*N)]\nhi = diffs[int(0.975*N)]\npoint = standardized_mean(closed) - manual_actual_mean\nprint(f"Standardized assist mean ttc minus manual actual mean ttc (minutes): point={point:.1f}, 95% bootstrap CI=({lo:.1f}, {hi:.1f})")\nprint(f"in hours: point={point/60:.2f}h, CI=({lo/60:.2f}h, {hi/60:.2f}h)")\nprint("Does CI include 0 (no difference)?", lo<=0<=hi)\nprint("Does CI include the 'claimed improvement' direction (negative, i.e. assist faster)?", hi<0)\n\nprint()\nprint("=== Deterministic bound check: sev1 manual max CLOSED ttc vs sev1 assist OPEN current ages ===")\nsev1_manual_max = max(closed[('manual','sev1')])\nsev1_assist_open_ages = sorted(open_ages[('assist','sev1')])\nprint(f"manual sev1 max closed ttc: {sev1_manual_max/60:.1f}h")\nprint(f"assist sev1 currently-open ages (h): {[round(a/60,1) for a in sev1_assist_open_ages]}")\nprint(f"even the LEAST mature open assist sev1 incident is already {min(sev1_assist_open_ages)/60:.1f}h old, "\n      f"vs manual's sev1 max closed of {sev1_manual_max/60:.1f}h -> ratio {min(sev1_assist_open_ages)/sev1_manual_max:.1f}x")\n\nsev1_assist_closed_max = max(closed[('assist','sev1')])\nprint(f"assist sev1 max CLOSED ttc (for reference): {sev1_assist_closed_max/60:.1f}h")\nEOF\n
exit=1
```

### arm c

```text
PREREG_WRITE: ordinal 16 (Write -> <SCRATCH>/s15-arms/c/ledger.md)
CONTENT-CHECK REQUIRED: confirm from the events stream (extract_evidence.py events <transcript> --ordinal 16) that this write already carries the hypothesis table and predictions — a placeholder written early and filled after analysis is a reconstruction, not a preregistration.
CLASSIFY: 6 tool_use(s) precede the ledger write; classify each as orientation or analysis in the evidence artifact:
  - ordinal 4: Bash [ok] ls -la <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/ && echo "---" && wc -l <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/*.csv
  - ordinal 5: Read [ok] <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/PROBLEM.md
  - ordinal 6: Read [ok] <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/incidents.csv
  - ordinal 7: Read [ok] <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/activity.csv
  - ordinal 8: Read [ok] <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/staffing.csv
  - ordinal 13: Bash [ok] cd <SCRATCH>/s15-arms/c && wc -l <REPO_ROOT>/hypothesis-driven-analysis/tests/fixtures/s15-assist-rollout/*.csv
exit=1
```

## Content checks on the first ledger writes

Per the checker's mandatory `CONTENT-CHECK REQUIRED` line, each first ledger write was recovered verbatim from the events stream (`extract_evidence.py events <transcript> --ordinal N`).

**Arm a, Write @ ordinal 19 (last tool call, 17,287 chars):** the write already carries completed test outcomes — outcome-cell token counts over the write content: `NOT_TESTED` 0, `CONSISTENT` 5, `CONTRADICTED` 2, `UNRESOLVED` 5 — and a filled Conclusion.
This is a conclusion-time reconstruction, not a preregistration; the ledger's own process note says the same ("initial orientation … proceeded straight into inspecting the workflow-vs-outcome relationship … before this ledger was written").

**Arm b, Write @ ordinal 14 (last tool call, 17,684 chars):** same shape — `NOT_TESTED` 0, `CONSISTENT` 8, `CONTRADICTED` 3, `UNRESOLVED` 3, filled Conclusion; reconstruction, matching the ledger's "Note on process".

**Arm c, Write @ ordinal 16 of 21 (10,314 chars):** a genuine plan.
The write carries the full hypothesis table with necessary predictions (verbatim header: `| id | claim | Candidate explanation | Prediction if true | Prediction if false | Necessary prediction (failure refutes) | Cheapest adequate test | Data needed |`), a Sources table, the full Data Validity section including the completeness-semantics entry, seven Tests rows all reading `NOT_TESTED` with empty Evidence cells, `## Amendments` "(none yet)", and `## Conclusion` reading exactly `(filled in after tests run)`.
Outcomes were filled by the Edit @ ordinal 21 after `tests.py` ran at ordinals 17–18.
The assistant text between ordinals 15 and 16 reads: "Now let me write the investigation ledger before running any outcome-relationship tests, per the skill's preregistration rule." — consistent with, but not the basis of, the machine-established ordering.

## Classification of pre-write touches

Orientation = schema, row counts, coverage, covariate balance (permitted before preregistration); analysis = any exposure-outcome contrast (forbidden).
Every row listed by the checker is classified below, plus the script-indirection rows the checker could not list (see the instrument observation at the end).

### arm a (ledger write @ 19; verdict: analysis preceded the write — assertion 10 FAIL)

| ordinal | listed by checker | touch | class |
| --- | --- | --- | --- |
| 2 | yes | `ls -la` references dir + fixture dir | orientation |
| 3 | yes | Read `PROBLEM.md` | orientation |
| 6 | yes | `head`/`wc`/`cat` of the three CSVs | orientation |
| 7 | yes | value counts (service_group, workflow, severity, intake_channel, date range) | orientation |
| 8 | yes | service_group counts | orientation |
| 12 | **no — script indirection** | `python3 orient.py`: row counts, open-incident breakdowns by workflow/group/severity/day, per-day incident counts; no outcome field aggregated | orientation |
| 14 | **no — script indirection** | `python3 analysis.py`: TTC and responder-minutes by workflow, by severity × workflow, handoffs, reopen rates — exposure-outcome contrasts | **analysis** |
| 16 | **no — script indirection** | `python3 analysis2.py`: closed_at-to-extract gaps per workflow, reopen-window maturity — outcome-adjacent | **analysis** |
| 17 | yes | heredoc: reopen rates by severity × workflow and channel × workflow | **analysis** |
| 18 | yes | heredoc: sev1 TTC contrasts, overlap checks across strata | **analysis** |

### arm b (ledger write @ 14; verdict: analysis preceded the write — assertion 10 FAIL)

| ordinal | listed by checker | touch | class |
| --- | --- | --- | --- |
| 3 | yes | `ls`/`wc -l` of fixture CSVs | orientation |
| 4 | yes | Read `PROBLEM.md` | orientation |
| 5 | yes | headers + staffing.csv contents | orientation |
| 6 | yes | row counts, value counts, workflow-by-date | orientation |
| 7 | yes | missing-activity join list by workflow/severity | orientation (coverage) |
| 8 | yes | closed-only TTC median/mean by workflow, open ages | **analysis** |
| 9 | yes | severity-stratified TTC/responder-minutes/handoffs/reopen by workflow | **analysis** |
| 10 | yes | open-rate matrix + direct standardization of TTC/responder-minutes | **analysis** (matrix half is coverage; the standardization is an exposure-outcome contrast) |
| 11 | yes | handoffs/reopen by severity × workflow, staffing week averages | **analysis** |
| 12 | yes | day-by-day medians, channel-stratified medians | **analysis** |
| 13 | yes | bootstrap CI on the standardized contrast, deterministic sev1 bound | **analysis** |

### arm c (ledger write @ 16; verdict: all pre-write touches orientation — assertion 10 PASS)

| ordinal | listed by checker | touch | class |
| --- | --- | --- | --- |
| 4 | yes | `ls`/`wc -l` of fixture CSVs | orientation |
| 5 | yes | Read `PROBLEM.md` | orientation |
| 6 | yes | Read `incidents.csv` (raw) | orientation, with caveat below |
| 7 | yes | Read `activity.csv` (raw) | orientation, with caveat below |
| 8 | yes | Read `staffing.csv` (raw) | orientation, with caveat below |
| 12 | **no — script indirection** | `python3 analysis/load.py`: row counts, missing-activity list, orphan-row check | orientation (coverage reconciliation) |
| 13 | yes | `wc -l` of fixture CSVs | orientation |
| 15 | **no — script indirection** | `python3 analysis/orient.py`: schema/value checks, cutover sanity, severity/group/channel mix by workflow (labeled "covariate balance, no outcome"), coverage grids, field population, closed_at-before-opened_at integrity, maturation ages of open incidents | orientation (no exposure-outcome contrast anywhere in the script) |

Caveat on ordinals 6–8: raw CSV Reads expose outcome fields (`closed_at`, `responder_minutes`) to inspection before the write; they are classified orientation per suite precedent because no aggregation or contrast was computed, but a preregistration behind raw-content reads is weaker than one behind schema-only orientation, and this is recorded rather than waved off.

## Verbatim `Source completeness semantics` entries

The field `c7a3179` added to `references/ledger-template.md`, quoted in full from each arm's final ledger Data Validity section.

**arm a** (`s15-arms/a/ledger.md`):

> - Source completeness semantics: for S2, an incident_id present in S1 but absent from S2 means "not yet closed as of the 2026-06-22T23:59Z extract" — not "event absent" and not export corruption — this reading is supported by two pieces of evidence: (a) every missing row's `opened_at` is well inside the collection window (no missing rows from incidents that would already have a `closed_at` past the extract), and (b) the age-so-far of every missing incident (8.5–17.2 days as of the extract) is internally consistent with "still working it," not with a gap in export mechanics (there is no missing row for an incident that any comparable-severity peer closed in). This reading is inferred from internal consistency, not an independent source, so it is asserted with that caveat rather than treated as externally confirmed.

**arm b** (`s15-arms/b/ledger.md`):

> - Source completeness semantics: in S2, absence of a row means "not yet closed as of the extract" (event pending, not event absent or export-dropped) — established by the closed-incident age benchmark above and by the fact that `incidents.csv` (S1) already lists these 11 incidents by ID with a real `opened_at`, so they are not missing rows, they are open ones. This licenses treating them as right-censored survivors, not as an unknown-direction missingness problem.

**arm c** (`s15-arms/c/ledger.md`; byte-identical in the ordinal-16 plan write):

> - Source completeness semantics: S2 (activity.csv) — an absent row means the incident had not closed as of 2026-06-22T23:59Z (this is the export's stated contract per PROBLEM.md: "activity.csv contains the recorded resolution activity as of the extract"); it does not mean the event is absent or unrecorded-but-happened. The still-open incidents have been open 8 to 17 days as of the extract (all past the stated 7-day maturation floor), so "not yet matured" does not explain the gap — these are incidents that have had enough time to close and have not.

Adjudication basis recorded here so the verdicts are checkable: arm b's cited evidence is closure-status-invariant — an absent-row incident's age-at-extract is `extract − opened_at` whether the incident is still open or closed-but-unrecorded, and S1 listing the incident says nothing about S2's recording — so it cannot establish the reading it claims to establish (assertion 6 FAIL); arm a's missingness-shape argument can partially discriminate (random export drops would not concentrate in one arm's hardest cell) and is explicitly caveated as internal-only (borderline PASS); arm c cites the one documented statement about the source and scopes its maturity note to timing only (PASS, with the over-read weakness that "recorded resolution activity" describes content, not recording completeness).

## score_ledger.py outputs (verbatim)

### arm a (`--final` only; the run kept no Plan-time ledger)

```text
note: --plan omitted; C2 (status laundering) not checked
OK:
  - C1 checked and passed: 5 summary row(s) read, 0 REFUTED, none of them causal
  - C2 NOT CHECKED: no --plan supplied; status laundering was not verified
exit=0
```

### arm b (`--final` only; the run kept no Plan-time ledger)

```text
note: --plan omitted; C2 (status laundering) not checked
FAIL:
  - parse: final ledger: H2 (retrospective, descriptive) has an unrecognized status cell: 'best supported'
  - parse: final ledger: H3 (retrospective, data-artifact) has an unrecognized status cell: 'best supported'
  - parse: final ledger: H5 (retrospective, descriptive) has an unrecognized status cell: 'best supported'
exit=1
```

C1 never evaluated for arm b: the scorer fails closed at parse on three `best supported` status cells — the Third wave's issue #73-class defect recurring after the Fourth-wave template fix.
The manual read of the same table (H1 `causal` / `UNRESOLVED`) is recorded in the run file as unscoreable-by-machine, not as a machine pass.

### arm c (`--final` + `--plan` recovered verbatim from the ordinal-16 Write)

```text
OK:
  - C1 checked and passed: 4 summary row(s) read, 0 REFUTED, none of them causal
  - C2 nothing to check: the final ledger carries no REFUTED descriptive or data-artifact row, so no laundering was possible to detect or rule out
exit=0
```

## Key memo quotes (verbatim from the `text` extraction)

**arm a:** "11 of 420 incidents are still open at extract time (7–17 days after opening). 10 of those 11 are in the assist arm … This *understates* how bad assist-sev1 is, since the slowest cases aren't even in the \"closed\" comparison above."
Also: "Full methodological caveat: some of this was found through exploratory grouping before hypotheses were formally written down (the severity-composition and censoring findings are `retrospective` in this analysis's own record) …" — the memo admits the retrospective framing the machine check confirmed.

**arm b:** "Of 420 incidents, 11 are still open at extract time … Those 5 open sev1 incidents are already 205-360 hours old … No matter how they eventually resolve, they can only push Assist's true sev1 performance further behind manual; excluding them (as a naive \"median time to close\" does) manufactures an improvement that isn't there."
Also: "All hypotheses are labeled retrospective (orientation surfaced the cause-outcome pattern before the table was written) …" — same admission, same machine confirmation.

**arm c:** "Of 420 incidents, 11 are still unresolved at the extract, despite 8–17 days elapsed (well past the 7-day maturation floor)."
And the per-quantity direction discipline: "elapsed calendar time is a valid lower bound for eventual time-to-close, but not for responder-minutes (labor could in principle be idle time). Direction is known (Finance's estimate is missing exposure that skews toward the hardest, priciest cases); magnitude is not."

All three memos use "still open"/"still unresolved" rather than "no recorded closure"; assertion 6 was therefore adjudicated on each ledger's completeness-semantics entry per the amended pass rule, as recorded above.

## Instrument observation: script-indirection blind spot in check_prereg's data pattern

A pre-write Bash row that executes a scratch script (`python3 analysis.py`) carries only the script invocation as its manifest target, so `--data-pattern 's15-assist-rollout'` cannot see the fixture access that lives inside the script file.
Measured in this wave: five such rows across two arms — arm a ordinals 12/14/16 (two of which are analysis-class) and arm c ordinals 12/15 — were absent from the checker's CLASSIFY lists.
The pattern instrument itself works on positives: rows whose command text names the fixture directory (arm a 17/18, arm b 6–13) were listed.
Consequence: on arm a the omission was harmless (listed rows 17/18 already establish analysis-before-write), but a run that did all its pre-write analysis through scratch scripts would present the scorer a CLASSIFY list containing only innocuous rows — the checker's exit 1 still forces classification, but the list would be silently incomplete.
Mitigation applied this wave: every pre-write Bash row was classified regardless of pattern match, reading script files from the arms' scratch directories (tables above).
This is the same family as the Ninth wave's hidden-target fix (unextracted targets are always listed); a candidate follow-up — e.g. always listing pre-write Bash rows that execute files created by earlier Write ordinals, or requiring the scorer to classify all pre-write Bash rows — is recorded for the controller, not patched in this tests-only commit.
