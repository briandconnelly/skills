# Scenario 15 — Post-strengthening scoring, skill @ `b8602fb` (arm f)

Date: 2026-07-19 (dispatch; transcript tool timestamps 2026-07-19T14:44–14:53Z).
Run: with-skill Sonnet arm against skill @ `b8602fb`; fixture `tests/fixtures/s15-assist-rollout/`, digests recomputed unchanged at scoring (evidence artifact).
Transcript: `s15-post-f.jsonl`, sha256 `6a37f181…0f275a3`; scratch `s15-arms/f/` (`ledger.md` only — all analysis ran as inline Bash heredocs naming the fixture).
Assertion 6 is adjudicated under the assertion text as rewritten 2026-07-19 (see the Tenth wave's re-adjudication block).
Machine checks in `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md` (Post-strengthening section); nothing below is scored from the run's self-report.

| # | Assertion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Routes full; states the no-holdout cutover does not identify the causal effect | PASS | Full ledger; memo: "The pilot was a single simultaneous cutover for both service groups … no holdout. That alone means nothing here can identify Assist's causal effect". |
| 2 | Causal hypothesis left `UNRESOLVED` (machine: C1) | PASS | H1 `causal` / `UNRESOLVED` ("cannot mark a causal claim `REFUTED` from an unidentified contrast per the skill's rule"); `score_ledger.py` C1: `OK … 6 summary row(s) read, 1 REFUTED, none of them causal`, exit 0 — the one `REFUTED` is H0 (`descriptive`). |
| 3 | No conclusion-time descriptive `REFUTED` (machine: C2) | FAIL | H0 (`descriptive`) carries a conclusion-time `REFUTED` and no Plan-time ledger exists to verify it against: the only Write (ordinal 14) already carries completed outcomes, so `C2 NOT CHECKED` and the structural half fails closed as unverifiable — the whole hypothesis table, H0 included, was written after the analysis that refuted it. Estimand half alone is satisfied (H0 names its estimand and fails at it via T1, CIs excluding 0 in every tier). |
| 4 | Marginal median confirmed correct for its estimand; estimands distinguished | PASS | Memo: "Pooled across all incidents, median time-to-close did fall (436 → 326.5 min)"; Conclusion: "real as a number but is not evidence of faster handling"; adds a precision caveat (the marginal bootstrap CI straddles 0 once resampled) without calling the dashboard number false; stratified, reweighted, and pooled quantities kept distinct throughout T1/T3. |
| 5 | Case mix, staffing, calendar, outcome-recording completeness kept live; no causal winner | PASS | Strongest of the wave: "It is equally possible the tool is fine and the rollout period's capacity disruption … is what hurt performance"; uniquely flags the exact-lockstep daily severity split (4/12/14 → 2/6/22) as a threat to treating severity as a clean pre-treatment covariate; staffing/interruption, mix, censoring (H4), and week-collinearity all live; every non-H0 status `UNRESOLVED`. |
| 6 | Completeness semantics before event-status/direction claims (rewritten text) | FAIL | The entry says "established by" (a) the missing rows' elapsed-minutes signature ("a maturity/backlog signature, not an export gap") — the source's own missingness pattern, closure-status-invariant, which the strengthened rule names as never sufficient — and (b) the S3 `incidents_opened` ↔ S1 reconciliation, genuine cross-source evidence that establishes S1's row-completeness but says nothing about whether S2 recorded every closure; the entry's own final clause ("for incidents that have not concluded") assumes exactly what the evidence cannot show. Under the strengthened rule the entry reads `UNKNOWN`; instead event-status language runs throughout ("treated as still open … not as missing records" in Sources; "still unresolved" in the memo; "none of the 14 sev1 incidents … reached a durable close") and the memo asserts the direction flat ("Right-censoring inflates it further"). The Limitations bullet does carry a genuine estimand-specific bound adjacent to its direction claim ("bounded below by their current elapsed time (12,315–21,579 min for the sev1 cases)") — the best adjacency discipline of the three arms — but the bound is licensed by semantics never established, and the completeness assumption is named at no claim site, so the adjacency half cannot rescue the entry. |
| 7 | No responder-hours as identified effect | PASS | "The 'hours saved' figure is not a credible number to book; it's an artifact of case-mix and censoring"; no identified effect claimed in either direction; T2's consumed-hours observation reported without booking. |
| 8 | Stop rule after every promised stratum | PASS (nominal) | All five tests executed across all three severity tiers and both groups (T1/T5 cover every stratum); stop condition post-hoc — the ledger is a conclusion-time write (scored at #10, not double-punished here); the post-write ordinal-15 verification pass that resolved T3's denominator discrepancy is recorded as good practice. |
| 9 | Every secondary field interpreted, incl. the handoffs aggregation reversal | FAIL | Ordinal 10 computed marginal handoffs by workflow and ordinal 12 the per-stratum means, so the run held the full planted reversal in its outputs; ledger T5 interprets the strata ("sev1 mean handoffs also collapsed (5.93 → 0.44) while sev2/sev3 handoffs rose slightly" — "slightly" for sev3's near-tripling 0.54 → 1.52) but never reports the marginal figure, so the aggregation reversal (marginal improves while sev2/sev3 worsen) is never surfaced as a reversal; the memo omits handoffs. |
| 10 | Plan-time ledger written before first analysis query (machine: `check_prereg.py`) | FAIL | `check_prereg` exit 1: only ledger write is ordinal 14 of 16 (the ordinal-16 Edit corrects T3). Content check: the write already carries completed outcomes (`NOT_TESTED` 0, `CONSISTENT` 10, `CONTRADICTED` 1, `UNRESOLVED` 5, `REFUTED` 2) and a filled Conclusion — a reconstruction. Pre-write ordinals 10–13 are exposure-outcome contrasts (marginal + per-field outcomes by workflow at 10; stratified medians and mix-reweighting at 11; reopen/handoffs slices and censored-age listing at 12; bootstrap CIs at 13). The memo's "methodological note for the record" admits the crossing ("crossed into cause-outcome territory … before a formal hypothesis table was written") and the machine confirms it. |

Total: 6/10.

Conclusion correctness: correct — do not expand, do not book savings, causal effect not identified; mix-shift reversal (with the lockstep observation), censoring asymmetry, staffing/interruption co-exposures, and the 100% sev1 reopen cluster all caught.
Premature-conclusion flag: no.
Honesty note: the process note and `retrospective` labels are accurate self-description; as with the Tenth wave's arms a/b, the prereg instrument's job here reduced to confirming an honest confession.
Cost: 16 tool calls, 100,768 subagent tokens (harness-reported) — +98.0% over the Third-wave S15 baseline (50.9k).
Evidence: `tests/runs/artifacts/2026-07-18-scenario15-completeness-evidence.md`.
