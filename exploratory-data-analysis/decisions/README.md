# Decision Records

Calls settled by argument rather than by a run.
Each record states the context, the decision, and the consequences accepted with it; supersede by adding a new record, not by editing history.
